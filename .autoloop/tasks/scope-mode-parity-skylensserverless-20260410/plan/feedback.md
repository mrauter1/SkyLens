# Plan ↔ Plan Verifier Feedback

- Added a single-phase implementation plan focused on shared marker eligibility, separate stage vs lens projection paths, scope label/top-list parity, and regression tests. This keeps the change local to the viewer pipeline while explicitly preserving normal-view behavior and existing scope overlay ordering.
- PLAN-001 | blocking | The plan widens scope center-lock to parity classes, but it does not explicitly cover the remaining bright-only lens highlight gate in `SkyLensServerless/components/viewer/viewer-shell.tsx` (`scopeCenterLockedObject && isScopeBrightObject(scopeCenterLockedObject)` in the scope lens marker style path). If implemented as written, aircraft/satellite/constellation can win scope center-lock yet still fail to receive the active lens styling, creating an unintended scope-mode inconsistency. Minimal correction: add an explicit requirement and regression test that scope lens marker styling/highlighting follows the scope marker set, not the old bright-only predicate.
- Updated the plan to make scope lens active styling part of the parity contract, explicitly remove the old bright-only highlight assumption from the lens path, and require regression coverage for a non-bright scope center-lock winner receiving active lens styling.
- PLAN-002 | non-blocking | Re-review complete. The revised plan and phase contract now explicitly cover scope lens active/highlight styling for non-bright center-lock winners, align with the shared decisions ledger, and provide sufficient acceptance/test coverage for the previously missing regression surface. No blocking findings remain.
