# Plan ↔ Plan Verifier Feedback
- 2026-04-07: Replaced the empty plan with a single-slice implementation plan that keeps the existing limiting-magnitude gate intact, localizes the rendering change to deep-star alpha/radius mapping, and adds explicit regression controls for shared `scopeRender` consumers and no-halo canvas behavior.
- 2026-04-07: Refined the single-slice plan after code inspection to explicitly remove the existing viewer-shell aperture attenuation path from deep-star canvas alpha, keep aesthetic shaping out of the canvas, and call out the runtime tests that currently encode aperture-driven alpha behavior.
- PLAN-001 `blocking`: [plan.md] omits the user-provided default emergence-band and magnitude-to-radius tuning values from the implementation contract. Evidence: the request snapshot supplies explicit recommended defaults (`EMERGE_START`, `EMERGE_END`, smoothstep exponent `1.35`, `R_MIN`, `R_MAX`, `MAG_BRIGHT`, `MAG_FAINT`, `RADIUS_CURVE`) to achieve the target astrophotography look, but the current Milestones / Planned Interfaces / Validation sections only say "add helper" and "smooth progressive alpha" with no anchored parameters or acceptance thresholds. Failure scenario: implementers can choose materially different curves that still satisfy the broad prose but reintroduce threshold pop-in, late saturation, or flattened size variation, and the tests will have no concrete baseline to lock the intended behavior. Minimal correction: add the recommended default constants/formulas to the plan (or explicitly document an intentional deviation and why), and tie validation to those defaults so implementation and tests converge on the same emergence/radius behavior.
- 2026-04-07: Addressed `PLAN-001` by anchoring the plan and phase contract to the request-default emergence and radius constants/formulas, and by extending validation language so implementation/tests must lock those baselines unless a later turn explicitly justifies a retune.
- PLAN-002 `non-blocking`: Re-review found no remaining planning defects. `PLAN-001` is effectively closed because the narrative plan, phase contract, validation section, and shared decisions ledger now all align on the same default emergence/radius baselines and preserve the requested regression guards.
- 2026-04-07: Refreshed the plan after repository inspection so it reflects the actual starting state: the helper functions, explicit canvas `alpha`/`radius` contract, and core-only renderer already exist, so implement/test work should verify or tighten that path rather than re-architect it; the plan now also makes full `ScopeRenderProfile` compatibility explicit because shared consumers still validate those fields.
- PLAN-003 `non-blocking`: Verification re-audit found no remaining plan defects. The current narrative plan, single-phase contract, checked criteria, and shared decisions ledger consistently preserve the requested limiting-magnitude gate, magnitude-based radius, progressive emergence alpha, core-only rendering, and compatibility protections for shared `scopeRender` consumers.
