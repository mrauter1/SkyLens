<autoloop-decisions-header version="1" block_seq="1" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260406T023904Z-ba39616f" ts="2026-04-06T02:39:05.139271+00:00" />
Plan scope is the repo-root Next.js app only; the sibling SkyLensServerless tree is excluded by the active root tsconfig and is not part of this task unless a later clarification says otherwise.
Scope optics must execute only after the existing enabled-layer, likely-visible/daylight, and horizon gates, so the new logic can only further narrow star visibility when scope mode is enabled.
Filtered visibleStars must remain the source for constellation construction so scope mode cannot bypass star visibility through constellation overlays.
Include a mirrored Settings-sheet scope-mode toggle in addition to the quick-control toggle because the acceptance contract requires toggle synchronization between both surfaces.
<autoloop-decisions-header version="1" block_seq="2" owner="implementer" phase_id="scope-optics-foundation" pair="implement" turn_seq="1" run_id="run-20260406T023904Z-ba39616f" ts="2026-04-06T02:42:25.135422+00:00" />
normalizeVisibleStars keeps the new scope inputs optional and defaults scope mode off so existing callers and older tests remain backward compatible during the phased rollout.
The bundled stars_200 catalog tops out at roughly magnitude 3.12, so fixture-based celestial scenes cannot exercise the new limiting-magnitude cutoff; a mocked star-pipeline unit test covers actual scope filtering behavior instead.
<autoloop-decisions-header version="1" block_seq="3" owner="test_author" phase_id="scope-optics-foundation" pair="test" turn_seq="1" run_id="run-20260406T023904Z-ba39616f" ts="2026-04-06T02:52:34.085033+00:00" />
Phase test coverage explicitly exercises both storage directions for nested scope settings, because this phase changes persisted shape and write-path regressions would not be caught by read-only compatibility tests.
Phase test coverage explicitly checks that ViewerShell forwards persisted `scopeModeEnabled` and `scopeOptics` into `normalizeVisibleStars`, because scope-mode activation is wired through persisted viewer settings before the later UI-control phase lands.
Phase test coverage explicitly asserts that `scopeRender` metadata is absent when scope mode is off, because the legacy non-scope rendering path must remain untouched until the later renderer integration phase.
<autoloop-decisions-header version="1" block_seq="4" owner="implementer" phase_id="scope-optics-ui-rendering" pair="implement" turn_seq="1" run_id="run-20260406T023904Z-ba39616f" ts="2026-04-06T02:57:19.668933+00:00" />
Marker-scale remains a Settings-only display multiplier and is intentionally applied after both legacy marker sizing and scope photometry sizing so it stays separate from optical magnification.
ViewerShell consumes scopeRender by drawing a compact halo/core star marker only when star metadata provides scopeRender; stars without that metadata continue through the legacy magnitude-based marker path unchanged.
<autoloop-decisions-header version="1" block_seq="5" owner="test_author" phase_id="scope-optics-ui-rendering" pair="test" turn_seq="1" run_id="run-20260406T023904Z-ba39616f" ts="2026-04-06T03:07:29.581047+00:00" />
Phase UI coverage exercises quick-control aperture and magnification through DOM slider events, while settings-sheet-driven marker-scale coverage uses the canonical callback path exposed by the mocked sheet, so both intended control surfaces are locked without duplicating full-panel integration scaffolding.
