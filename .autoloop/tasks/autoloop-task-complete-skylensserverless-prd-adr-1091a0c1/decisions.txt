<autoloop-decisions-header version="1" block_seq="1" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260404T010403Z-37ca1abb" ts="2026-04-04T01:04:04.221107+00:00" />
Treat the SkyLensServerless/ package tree as the authoritative implementation target for this task; do not mirror scope-dataset changes into the duplicate repo-root app unless a later clarification explicitly requires parity.
Interpret PRD/ADR relative paths such as .cache/, public/, scripts/, tests/, and data/scope-source/ relative to SkyLensServerless/ so the package-local npm scripts, tests, and static export stay internally consistent.
Keep the new dataset pipeline additive to existing runtime features by writing only the committed dev artifacts to SkyLensServerless/public/data/scope/v1/ and keeping raw/staging/report files under ignored SkyLensServerless/.cache/ paths.
<autoloop-decisions-header version="1" block_seq="2" owner="planner" phase_id="task-global" pair="plan" turn_seq="2" run_id="run-20260404T010403Z-37ca1abb" ts="2026-04-04T01:07:17.928302+00:00" />
Superseding the prior turn, treat repository-root paths as authoritative for this task; do not reinterpret PRD/ADR-relative paths such as public/, .cache/, scripts/, tests/, or data/scope-source/ under the nested SkyLensServerless/ tree without an explicit clarification.
Treat the nested SkyLensServerless/ copy as reference-only for regression analysis and prior-art comparison; implement the dataset pipeline only at the active repository root unless a later clarification changes scope.
Use root AUTOLOOP_SERVERLESS_TASK.md as the default workflow-doc update target for the new scope:data commands unless implementation finds an existing repo-root README that is clearly a better home.
<autoloop-decisions-header version="1" block_seq="3" owner="implementer" phase_id="repository-wiring-and-core-contracts" pair="implement" turn_seq="1" run_id="run-20260404T010403Z-37ca1abb" ts="2026-04-04T01:09:58.346086+00:00" />
Implement the shared scope-data core as repo-root ESM `.mjs` modules so the new Node CLI entrypoints can consume one contract surface without adding a TypeScript runtime/transpile dependency.
Centralize exact ADR constants, schema validators, JSON object constructors, path helpers, name-table helpers, and tile helpers under `lib/scope-data/` and keep the root scripts thin so later download/build/verify phases reuse one deterministic source of truth.
<autoloop-decisions-header version="1" block_seq="4" owner="implementer" phase_id="repository-wiring-and-core-contracts" pair="implement" turn_seq="2" run_id="run-20260404T010403Z-37ca1abb" ts="2026-04-04T01:16:10.048463+00:00" />
Encode manifest/report band arrays as canonical-order tuples and band index schemas as per-band literal unions so the shared core rejects band-definition drift instead of leaving it to later script code.
Use strict zod object schemas for manifest, index, report, and nested objects so future verifier/build phases reject unknown keys through the same centralized contract layer.
<autoloop-decisions-header version="1" block_seq="5" owner="test_author" phase_id="repository-wiring-and-core-contracts" pair="test" turn_seq="1" run_id="run-20260404T010403Z-37ca1abb" ts="2026-04-04T01:18:14.733328+00:00" />
Cover AC-1 and AC-2 with direct repo-file assertions in `tests/unit/scope-data-contracts.test.ts` instead of git subprocess checks so the phase-local tests stay deterministic, offline, and independent of user/global git configuration.
<autoloop-decisions-header version="1" block_seq="6" owner="implementer" phase_id="downloader" pair="implement" turn_seq="1" run_id="run-20260404T010403Z-37ca1abb" ts="2026-04-04T01:19:26.882881+00:00" />
Treat a valid cached Tycho-2 `.gz` plus a missing or stale expanded text file as recoverable without network: regenerate `expanded/tyc2.dat.xx` from the cached raw gzip instead of forcing a re-download.
Keep the downloader summary and tests keyed to the repo-root CLI contract already wired in `lib/scope-data/cli.mjs`, including override precedence where caller-provided CLI or environment base URLs replace the built-in fallback list rather than appending to it.
<autoloop-decisions-header version="1" block_seq="7" owner="implementer" phase_id="builder" pair="implement" turn_seq="1" run_id="run-20260404T010403Z-37ca1abb" ts="2026-04-04T01:25:40.948150+00:00" />
When `scripts/build-scope-dataset.mjs --mode prod` cannot find a complete expanded Tycho-2 main-catalog set under repo-root `.cache/scope-source/tycho2/expanded/`, it emits the deterministic dev dataset instead of failing so the PRD fallback rule still holds without network.
Treat a manual override row whose normalized `displayName` is empty as an explicit unnamed override that suppresses the bright-star HIP fallback for that matched production source row.
Count `report.names.emittedRows` across cumulative band emission, not unique normalized source rows, so the report matches the shipped tile rows that actually reference non-zero `nameId` values.
<autoloop-decisions-header version="1" block_seq="8" owner="test_author" phase_id="builder" pair="test" turn_seq="1" run_id="run-20260404T010403Z-37ca1abb" ts="2026-04-04T01:36:16.124761+00:00" />
Lock the current builder decision that `buildScopeDataset({ mode: 'prod' })` emits the deterministic dev dataset when the expanded Tycho-2 cache is absent by covering it in the integration test rather than leaving that fallback behavior implicit.
<autoloop-decisions-header version="1" block_seq="9" owner="implementer" phase_id="verifier-docs-and-dev-artifacts" pair="implement" turn_seq="1" run_id="run-20260404T010403Z-37ca1abb" ts="2026-04-04T01:39:26.353348+00:00" />
Use `AUTOLOOP_TASK_SKYLENSSERVERLESS_COMPLETE_IMPLEMENTATION.md` as the repo-root workflow-note target for the `scope:data:*` commands because `AUTOLOOP_SERVERLESS_TASK.md` is a historical migration brief whose root-edit prohibition conflicts with the authoritative repo-root dataset implementation.
Treat this phase as coverage-and-documentation closure unless validation disproves the shipped pipeline: the existing root verifier/build code already satisfied the dataset/report contracts, and rebuilding the committed dev dataset produced no artifact diffs.
<autoloop-decisions-header version="1" block_seq="10" owner="test_author" phase_id="verifier-docs-and-dev-artifacts" pair="test" turn_seq="1" run_id="run-20260404T010403Z-37ca1abb" ts="2026-04-04T01:43:00.415351+00:00" />
Lock the dev-build report determinism at the raw-file level in `tests/unit/scope-data-build.integration.test.ts` so repeated builds must reproduce identical `.cache/scope-build/report.json` bytes, not only schema-equivalent parsed content.
