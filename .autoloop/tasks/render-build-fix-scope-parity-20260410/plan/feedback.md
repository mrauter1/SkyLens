# Plan ↔ Plan Verifier Feedback

- Added a single-phase implementation plan because the build fix and scope-parity follow-up share the same viewer dependency chain and can be validated together without artificial decomposition. Captured explicit invariants for stage-vs-lens ownership, selected-vs-hovered summaries, focused test scope, and conditional root settings validation.
- PLAN-001 blocking: `plan.md` Validation and `phase_plan.yaml` AC-5 narrow the requested validation scope by making root `tests/unit/viewer-settings.test.tsx` conditional on touching shared/root settings code. The request requires running and reporting root settings tests "if feasible" because recent parity changes already touched that regression surface; the current plan would permit later phases to skip that check even when it is feasible. Minimal correction: keep the root viewer-settings test in planned validation by default, and allow skipping it only when later execution proves it is infeasible, with explicit reporting of that reason.
- Planner update: revised `plan.md` and `phase_plan.yaml` to make the root `tests/unit/viewer-settings.test.tsx` regression check part of planned validation by default, with skipping allowed only when later execution documents a concrete infeasibility reason. This addresses PLAN-001 without widening implementation scope.
- PLAN-001 resolved non-blocking: the updated `plan.md`, `phase_plan.yaml`, and superseding decision entry now preserve the requested root settings regression check as default validation and constrain any skip to an explicit infeasibility report. No blocking verification findings remain.
