<autoloop-decisions-header version="1" block_seq="1" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260329T231101Z-4fd26a7b" ts="2026-03-29T23:11:01.316791+00:00" />
Treat explicit alignment focus on the live camera stage as the allowed exception to backdrop dismissal; settings and non-focus overlays should dismiss on outside click and Escape.
Drive single-next-step and single-primary-banner behavior from shared deterministic resolver outputs consumed by both desktop and mobile surfaces, not duplicated per-surface condition trees.
Do not add a separate UX-contract documentation artifact in this task; enforce the contract through local types/helpers and regression tests in the scoped codebase.
<autoloop-decisions-header version="1" block_seq="2" owner="planner" phase_id="task-global" pair="plan" turn_seq="2" run_id="run-20260329T231101Z-4fd26a7b" ts="2026-03-29T23:14:43.179704+00:00" />
Every dismissible overlay touched by this task must restore focus to its opener; if the opener unmounts during the close transition, restore focus to the nearest surviving control that reopens the same surface.
<autoloop-decisions-header version="1" block_seq="3" owner="implementer" phase_id="overlay-foundation" pair="implement" turn_seq="1" run_id="run-20260329T231101Z-4fd26a7b" ts="2026-03-29T23:16:37.168813+00:00" />
<autoloop-decisions-header version="1" block_seq="4" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260330T111642Z-f01accc6" ts="2026-03-30T11:16:42.751350+00:00" />
Converge the current compact live mobile overlay branch and the bespoke fallback mobile overlay branch onto one shared sheet structure instead of maintaining parallel DOMs with duplicated dismissal logic.
Resolve banner prioritization once in viewer-shell and feed the desktop warning stack plus mobile overlay content from that shared output; do not keep separate mobile fallback banner condition trees.
<autoloop-decisions-header version="1" block_seq="5" owner="implementer" phase_id="overlay-foundation" pair="implement" turn_seq="1" run_id="run-20260330T111642Z-f01accc6" ts="2026-03-30T11:18:40.502625+00:00" />
<autoloop-decisions-header version="1" block_seq="6" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260330T114315Z-364b5d58" ts="2026-03-30T11:43:15.573967+00:00" />
Preserve the existing mobile overlay and alignment test ids (`mobile-viewer-overlay*`, `mobile-alignment-overlay*`, `settings-sheet-*`, `alignment-start-action`) unless a selector change is required by the requested UX contract.
Treat the current `viewer-shell` split between desktop `topWarningItems` and mobile `FallbackBanner` branches as the banner-clutter source of truth; phase 2 should replace both with one shared prioritized resolver rather than retuning each surface separately.
<autoloop-decisions-header version="1" block_seq="7" owner="implementer" phase_id="overlay-foundation" pair="implement" turn_seq="1" run_id="run-20260330T114315Z-364b5d58" ts="2026-03-30T11:46:50.272981+00:00" />
ViewerShell now records opener/fallback focus targets locally for dismissible mobile overlays; alignment restore prefers the surviving mobile Align control before broader viewer reopen controls so close actions do not fall through to body focus.
The non-live fullscreen mobile viewer overlay path is replaced with the shared CompactMobilePanelShell contract; `mobile-viewer-overlay`, `mobile-viewer-overlay-shell`, `mobile-viewer-overlay-scroll-region`, and `mobile-viewer-overlay-compact-content` remain the stable regression hooks across states.
<autoloop-decisions-header version="1" block_seq="8" owner="implementer" phase_id="overlay-foundation" pair="implement" turn_seq="2" run_id="run-20260330T114315Z-364b5d58" ts="2026-03-30T12:07:14.354441+00:00" />
ViewerShell mobile-overlay regression tests must re-query `mobile-viewer-overlay-trigger` after the overlay closes because React remounts that trigger between the initial open and any later reopen path.
<autoloop-decisions-header version="1" block_seq="9" owner="test_author" phase_id="overlay-foundation" pair="test" turn_seq="1" run_id="run-20260330T114315Z-364b5d58" ts="2026-03-30T12:09:18.661452+00:00" />
For AC-3, the stable regression is to assert that explicit alignment focus removes the instruction-sheet shell/backdrop entirely, rather than trying to click a backdrop that should no longer exist.
<autoloop-decisions-header version="1" block_seq="10" owner="implementer" phase_id="guidance-and-banner-resolution" pair="implement" turn_seq="1" run_id="run-20260330T114315Z-364b5d58" ts="2026-03-30T12:11:28.375348+00:00" />
Alignment tutorial guidance now resolves to one explicit primary step plus at most one supporting note; waiting-for-motion keeps the existing `alignment-start-action` visible but disabled so the live-camera mobile flow keeps one dominant CTA without adding extra branches.
Desktop top warnings and mobile fallback banners now consume the same `resolveViewerBannerFeed(...)` output; primary selection prefers the highest-priority actionable banner when one exists, and the remaining items stay available behind one deterministic overflow disclosure.
The shared viewer banner resolver now consumes the live camera permission/status directly instead of a denied-only boolean so both `denied` and `unavailable` live non-camera states preserve the same fallback banner contract.
Phase-local unit coverage treats `cameraStatus: null` as the startup-pending no-banner edge and `cameraStatus: 'unavailable'` as the preserved non-camera fallback path so later turns do not silently normalize either case away.
<autoloop-decisions-header version="1" block_seq="11" owner="implementer" phase_id="desktop-hierarchy-and-regression" pair="implement" turn_seq="1" run_id="run-20260330T114315Z-364b5d58" ts="2026-03-30T12:33:49.386453+00:00" />
Desktop viewer hierarchy now uses one dominant next-action card ahead of the compact secondary action row; `desktop-open-viewer-action` stays stable by living in the primary card only when opening the viewer is the recommended next step, otherwise it remains in the secondary controls for focus-restore fallbacks.
Landing keeps the existing privacy/build facts but demotes them into a lighter support column so the live-viewer CTA remains the single dominant entry path while demo mode stays available as a secondary route.
<autoloop-decisions-header version="1" block_seq="12" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260330T124815Z-e50f7f42" ts="2026-03-30T12:48:15.818071+00:00" />
Treat the current codebase as already containing the intended shared mobile sheet, shared banner resolver, single-next-step model, and reduced desktop hierarchy; this run should be validation-led and limited to concrete gap fixes plus passing touched tests.
Do not reintroduce the retired fullscreen mobile overlay branch or separate mobile banner decision trees while fixing regressions in this run.
<autoloop-decisions-header version="1" block_seq="13" owner="implementer" phase_id="finalize-ux-contract" pair="implement" turn_seq="1" run_id="run-20260330T124815Z-e50f7f42" ts="2026-03-30T12:52:08.378999+00:00" />
Scoped finalize-ux-contract validation found no source regression in the current overlay, focus-restore, next-step, banner, or desktop-hierarchy implementation; preserve the landed implementation and treat Playwright failure in this container as an environment dependency gap (`libatk-1.0.so.0`), not an app-behavior signal.
<autoloop-decisions-header version="1" block_seq="14" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260330T151630Z-6594248c" ts="2026-03-30T15:16:30.474160+00:00" />
Do not mark `finalize-ux-contract` complete from subset-only `viewer-shell` evidence; the full `tests/unit/viewer-shell.test.ts` suite must finish and pass.
Treat missing Playwright Chromium libraries as an environment blocker that must be cleared or bypassed with a provisioned validation environment before phase completion, not as acceptable completion evidence.
<autoloop-decisions-header version="1" block_seq="15" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260330T162417Z-e57f4091" ts="2026-03-30T16:24:17.162415+00:00" />
Treat `/workspace/SkyLens/SkyLensServerless` as the authoritative app package for this task; do not drift into the duplicated root-level app/components/lib/tests tree while closing the UX blockers.
AC-5 validation evidence must come from the scoped `SkyLensServerless` package context after JS dependency bootstrap and with Chromium runtime support available; missing installs or browser libraries are environment/setup blockers, not product-behavior signals.
<autoloop-decisions-header version="1" block_seq="16" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260330T183135Z-a086c089" ts="2026-03-30T18:31:35.193681+00:00" />
Treat the required Chromium Playwright validation as currently passing in the authoritative `SkyLensServerless` package; the remaining completion blocker for this run is the full `tests/unit/viewer-shell.test.ts` suite not exiting normally.
Do not broaden product scope to clear the final blocker; isolate and fix the `viewer-shell` full-suite open-handle or teardown path in `SkyLensServerless/tests/unit/viewer-shell.test.ts` or its directly exercised viewer code, then rerun the preserved passing suites from `/workspace/SkyLens/SkyLensServerless`.
<autoloop-decisions-header version="1" block_seq="17" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260331T032150Z-4a2cbc14" ts="2026-03-31T03:21:50.526288+00:00" />
Bootstrap dependencies inside `/workspace/SkyLens/SkyLensServerless` before interpreting Vitest or Playwright failures for this run; missing local binaries or browser assets are setup blockers, not UX regressions.
Record authoritative validation with package-local binaries from `/workspace/SkyLens/SkyLensServerless` after bootstrap rather than relying on `npm test` in an unbootstrapped package context.
<autoloop-decisions-header version="1" block_seq="18" owner="implementer" phase_id="finalize-ux-contract" pair="implement" turn_seq="1" run_id="run-20260331T032150Z-4a2cbc14" ts="2026-03-31T03:24:31.719487+00:00" />
Keep the remaining `viewer-shell` acceptance fix scoped to the authoritative test harness: clear pending fake timers only after root cleanup, make the custom RAF helper clear queued timeout-backed frame handles on restore, and avoid production-code churn for this blocker.
Stabilize the two late regressions with local one-shot mocks rather than new shared helpers: use a scheduled/cancellable `requestVideoFrameCallback` mock for the video-frame path and a `mockImplementationOnce(...)` orientation emission for the relative-sensor path.
<autoloop-decisions-header version="1" block_seq="19" owner="implementer" phase_id="finalize-ux-contract" pair="implement" turn_seq="2" run_id="run-20260331T032150Z-4a2cbc14" ts="2026-03-31T03:42:01.911203+00:00" />
Stabilize the remaining estimated-aircraft selected-detail regression with the existing fake-timer plus `installAnimationFrameClock()` harness so the demo scene clock stays under explicit test control in the full `viewer-shell` suite instead of depending on real-time animation.
<autoloop-decisions-header version="1" block_seq="20" owner="test_author" phase_id="finalize-ux-contract" pair="test" turn_seq="1" run_id="run-20260331T032150Z-4a2cbc14" ts="2026-03-31T03:48:11.000000+00:00" />
For the selected-aircraft detail regression, assert both the rendered detail badge and the clicked marker’s `aria-pressed="true"` state so the test covers selection state and panel state together without adding a second selection-path test.
