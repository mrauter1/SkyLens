# Plan ↔ Plan Verifier Feedback
- Added the first implementation-ready plan with three ordered phases: overlay foundation, shared guidance/banner resolution, and desktop hierarchy/test hardening.
- Locked the key risk controls into the plan: backdrop-dismiss exception only for explicit alignment focus, shared deterministic resolver outputs across desktop/mobile, and no scope drift into unrelated refactors or docs-only artifacts.
- PLAN-001 `blocking`: The plan adds outside-click dismissal to viewer-owned overlays but only specifies focus restoration for `SettingsSheet`. Affected sections: `plan.md` milestone 1, compatibility/regression notes, and validation approach; `phase_plan.yaml` overlay-foundation acceptance criteria. Failure scenario: closing the mobile viewer sheet or alignment sheet by backdrop/escape can leave keyboard focus on `body` or a detached element instead of returning to the invoking control, which violates the request’s accessibility/focus-management requirement and its explicit focus-restoration regression coverage. Minimal correction: define focus-restore expectations for each dismissible overlay surface (for example, mobile viewer trigger, mobile Align action, desktop opener where applicable) and add matching unit/e2e validation.
- Updated the plan to resolve `PLAN-001`: overlay foundation now defines per-surface focus restoration, fallback behavior when the original opener unmounts, and matching unit/e2e validation expectations for viewer-owned overlays.
- PLAN-001 `non-blocking`: Re-verification passed after the planner revision. The plan now explicitly covers per-surface focus restoration, fallback opener handling, and matching validation for dismissible viewer overlays, so there are no remaining blocking verification findings.
- Refined the plan after code inspection: phase 1 now explicitly converges the two current mobile overlay DOM branches onto one shared sheet contract, and phase 2 explicitly replaces the separate mobile banner stacks with the same resolved banner output used by desktop.
- PLAN-002 `non-blocking`: Re-verification passed after the code-informed refinement. The current plan covers all requested UX outcomes, preserves the shared decision log constraints, keeps runtime-owned `phase_plan.yaml` metadata intact, and introduces no new blocking verification findings.
- Refreshed the plan against the current codebase for `run-20260330T114315Z-364b5d58`: it now names the exact duplicated `viewer-shell` mobile overlay branches, clarifies the per-surface focus-restore contract, anchors shared resolver work to the existing `topWarningItems` / `FallbackBanner` split, and points regression work at the current unit/e2e suites and stable selectors already in use.
- PLAN-003 `non-blocking`: Re-verification passed for `run-20260330T114315Z-364b5d58`. The refreshed plan preserves intent fidelity, keeps the runtime-owned `phase_plan.yaml` metadata valid, respects the shared decisions ledger, and leaves no blocking planning gaps for the implementer.
- Refreshed the plan for `run-20260330T124815Z-e50f7f42` after code inspection: the scoped UX work is already present in the codebase, so the plan now treats this run as a single validation-led completion slice focused on regression testing, targeted gap fixes, and preserving the current shared overlay/banner resolver seams.
- PLAN-004 `non-blocking`: Verification passed for `run-20260330T124815Z-e50f7f42`. The single-phase completion plan remains intent-faithful and behaviorally safe because it preserves the existing shared overlay/resolver seams, keeps the runtime-owned `phase_plan.yaml` metadata intact, respects the current decisions ledger, and requires the touched unit/e2e suites to prove dismissal, focus-restore, next-step, banner-priority, desktop-hierarchy, and mobile-overlay regressions before completion.
- Refreshed the plan for `run-20260330T151630Z-6594248c` after reviewing the active implementation feedback: the scoped UX contract still matches the codebase, but the completion slice now explicitly treats the full `tests/unit/viewer-shell.test.ts` run and Chromium-capable Playwright execution as mandatory blockers to close before `finalize-ux-contract` can be marked complete.
- PLAN-005 `non-blocking`: Re-verification passed for `run-20260330T151630Z-6594248c`. The refreshed single-phase plan stays intent-faithful, keeps runtime-owned `phase_plan.yaml` metadata valid, respects the current decisions ledger, and now makes the active completion blockers explicit instead of allowing silent closure from subset-only unit evidence or under-provisioned Playwright runs.
- Refreshed the plan for `run-20260330T162417Z-e57f4091` after auditing the workspace layout: the completion slice still stays validation-led, but it now makes the authoritative package boundary (`/workspace/SkyLens/SkyLensServerless`) and dependency-bootstrap prerequisite explicit so later phases do not validate the duplicated root tree or misread missing installs as product regressions.
- PLAN-006 `non-blocking`: Re-verification passed for `run-20260330T162417Z-e57f4091`. The current plan remains intent-faithful and behaviorally safe: it preserves the single-phase validation-led completion slice, keeps runtime-owned `phase_plan.yaml` metadata intact, stays aligned with the shared decisions ledger, and now explicitly constrains later turns to the authoritative `SkyLensServerless` package plus the required dependency/bootstrap and Chromium-capable validation prerequisites.
- Refreshed the plan for `run-20260330T183135Z-a086c089` after reviewing the active implementation feedback and current scoped sources: Playwright is no longer an active blocker in the authoritative package context, so the plan now narrows the remaining completion work to the full `tests/unit/viewer-shell.test.ts` exit/hang path while preserving the already-passing resolver/settings/e2e validation baseline.
- PLAN-007 `non-blocking`: Re-verification passed for `run-20260330T183135Z-a086c089`. The refreshed plan remains intent-faithful and behaviorally safe: it keeps the runtime-owned `phase_plan.yaml` metadata valid, stays aligned with the shared decisions ledger, preserves the already-cleared Playwright validation as baseline evidence rather than an active blocker, and correctly narrows the remaining completion work to the full `tests/unit/viewer-shell.test.ts` suite exiting normally from the authoritative `SkyLensServerless` package context.
- Refreshed the plan for `run-20260331T032150Z-4a2cbc14` after re-auditing the live package snapshot: the scoped UX implementation is still present, but the authoritative `SkyLensServerless` package currently lacks installed local test binaries, so the completion slice now starts with package bootstrap and explicit package-local Vitest/Playwright commands before treating any remaining failures as product regressions.
- PLAN-008 `non-blocking`: Re-verification passed for `run-20260331T032150Z-4a2cbc14`. The bootstrap-first refresh remains intent-faithful and behaviorally safe: it preserves the single-phase scoped UX completion slice, keeps the runtime-owned `phase_plan.yaml` metadata intact, stays aligned with the shared decisions ledger, and correctly treats missing package-local runners as setup blockers before resuming the already-identified full `tests/unit/viewer-shell.test.ts` acceptance blocker.
