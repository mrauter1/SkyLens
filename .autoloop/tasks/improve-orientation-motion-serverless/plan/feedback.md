# Plan ↔ Plan Verifier Feedback

- Replaced the empty plan artifacts with a two-phase implementation plan centered on the real seams in `orientation.ts` and `viewer-shell.tsx`, because the source spec requires both a runtime-coordinator rewrite and a readiness/UI/test rewrite.
- PLAN-001 `blocking`: The plan’s compatibility/config section and decisions ledger say to preserve the existing `Permissions-Policy` and iframe `allow` contract “as-is”, but the source-of-truth spec explicitly requires a Next.js header of `accelerometer=(self), gyroscope=(self), magnetometer=(self)` and embed delegation of only `accelerometer; gyroscope; magnetometer`, with “Do not broaden the allowlist.” Affected artifacts: `plan.md` Compatibility/config milestone + Compatibility Notes, `phase_plan.yaml` phase `viewer-integration-and-verification`, and the appended decisions block. Failure mode: implementation can faithfully follow this plan and still ship a broader policy than the spec allows. Minimal correction: update the plan to reconcile the existing camera/geolocation policy with the spec’s exact sensor-only contract, or explicitly surface a required clarification if keeping camera/geolocation is truly necessary.
- PLAN-002 `blocking`: The source spec requires orientation lifecycle handling: stop providers on `document.visibilitychange` to hidden and `pagehide`, then restart arbitration on visible and `pageshow`, plus preserve correct event-provider screen correction behavior across rotation. That requirement is not called out in `plan.md` milestones, regression controls, or `phase_plan.yaml` acceptance criteria. Failure mode: implementation can satisfy the current plan while still freezing or failing to recover after background/foreground transitions on Safari/Samsung-class browsers. Minimal correction: add lifecycle/restart handling and rotation-correction validation explicitly to the runtime phase scope and acceptance criteria.
- Revised the plan to resolve PLAN-001 and PLAN-002 by replacing the “preserve existing policy as-is” language with the spec’s exact sensor-only `Permissions-Policy`/iframe `allow` contract and by making visibility/page lifecycle plus rotation-correction handling an explicit runtime milestone, acceptance criterion, and regression surface.
- PLAN-003 `non-blocking`: Re-review complete. `plan.md`, `phase_plan.yaml`, and the current decisions block now reflect the narrowed sensor-only permissions-policy/embed contract and the required hidden/pagehide plus visible/pageshow lifecycle handling. No additional findings remain.
- Tightened the current run’s plan artifacts to name the concrete permission/embed validation surfaces that must move with the config change (`app/embed-validation/page.tsx` plus affected unit/e2e tests), because the repository still ships copy and assertions for the older camera/geolocation+motion contract and that drift would otherwise survive implementation.
- PLAN-004 `non-blocking`: Final verifier pass complete for run `run-20260328T220654Z-04289b02`. The plan, phase contract, criteria, feedback history, and shared decisions are internally consistent with the source spec, and no further blocking findings remain.
