<autoloop-decisions-header version="1" block_seq="1" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260328T220112Z-4c4bb5f7" ts="2026-03-28T22:01:12.541972+00:00" />
Keep public orientation source labels coarse; Safari compass validation must upgrade/downgrade through the existing deviceorientation absolute/relative labels instead of introducing a public compass-specific source.
`requestOrientationPermission()` is prompt-only in the new contract; real readiness must come from emitted provider samples, so `ViewerShell` must keep route orientation unknown until the first usable sample arrives.
The required Permissions-Policy and iframe allow contract already exists in `next.config.ts`, `public/_headers`, and embed validation; preserve that exact allowlist rather than broadening or redesigning it.
<autoloop-decisions-header version="1" block_seq="2" owner="planner" phase_id="task-global" pair="plan" turn_seq="2" run_id="run-20260328T220112Z-4c4bb5f7" ts="2026-03-28T22:04:46.822486+00:00" />
Supersedes the prior turn’s header note about preserving the existing Permissions-Policy/embed contract: implementation should align those artifacts to the spec’s sensor-only accelerometer/gyroscope/magnetometer allowlist instead.
Lifecycle handling is required scope: stop orientation providers on hidden/pagehide, restart arbitration on visible/pageshow, and keep event-provider screen correction rotation-aware without extra correction for screen-frame sensors.
<autoloop-decisions-header version="1" block_seq="3" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260328T220654Z-04289b02" ts="2026-03-28T22:06:54.242755+00:00" />
The source spec intentionally narrows the shipped Permissions-Policy and iframe allow contract to accelerometer/gyroscope/magnetometer only; update embed-validation copy and related tests in the same slice so the public contract does not drift.
`requestOrientationPermission()` becoming prompt-only does not make orientation ready: after a `granted` or `unavailable` prompt result, ViewerShell should keep route orientation `unknown` until a real sample arrives or startup conclusively fails.
Existing orientation runtime tests may be consolidated, but the implementation must preserve separate math/foundation coverage and add explicit browser-matrix, failover, and lifecycle cases from the source spec instead of relying on the current probe-based tests alone.
<autoloop-decisions-header version="1" block_seq="4" owner="implementer" phase_id="orientation-runtime-coordinator" pair="implement" turn_seq="1" run_id="run-20260328T220654Z-04289b02" ts="2026-03-28T22:09:26.383977+00:00" />
Safari compass validation downgrades back to `deviceorientation-relative` only after 3 consecutive bad compass-backed samples, and only when the current `deviceorientation-absolute` selection came from the validated compass path rather than a real absolute provider.
`lib/viewer/settings.ts` now accepts `relative-sensor` in persisted calibration metadata to keep the new public orientation source union from breaking calibration restore/storage on dependent flows.
<autoloop-decisions-header version="1" block_seq="5" owner="implementer" phase_id="orientation-runtime-coordinator" pair="implement" turn_seq="2" run_id="run-20260328T220654Z-04289b02" ts="2026-03-28T22:21:07.912597+00:00" />
The coordinator now keeps internal provider identities for `deviceorientationabsolute`, plain `deviceorientation` absolute, Safari validated compass absolute, and relative events so the required ladder can be enforced without changing the public `OrientationSource` labels.
Sensor permission hints are time-boxed during startup; only prompt denied hints that arrive within the startup grace window suppress sensor construction, while slow or unsupported Permissions API responses no longer cost higher-priority sensors the 500 ms arbitration window.
<autoloop-decisions-header version="1" block_seq="6" owner="test_author" phase_id="orientation-runtime-coordinator" pair="test" turn_seq="1" run_id="run-20260328T220654Z-04289b02" ts="2026-03-28T22:25:40.464417+00:00" />
Rotation coverage now explicitly asserts that `orientationchange` reprocesses the latest event-backed sample with the new screen angle, while screen-frame sensor samples remain single-corrected and therefore must not emit a duplicate pose on the same rotation event.
The Safari compass regression test is now anchored to the quaternion-derived heading for its “aligned” fixture, so a remaining red result on the upgrade/downgrade sequence should be treated as coordinator drift rather than a bad mocked sample.
<autoloop-decisions-header version="1" block_seq="7" owner="implementer" phase_id="viewer-integration-and-verification" pair="implement" turn_seq="1" run_id="run-20260328T220654Z-04289b02" ts="2026-03-28T22:36:35.832084+00:00" />
ViewerShell now treats `requestOrientationPermission()` as prompt-only: `granted`/`unavailable` prompt results keep route orientation `unknown` until a real sample arrives, while a startup timeout resolves `unknown` to `denied` when orientation APIs exist and `unavailable` only when no event/sensor APIs exist at all.
Selected-provider diagnostics are preserved across benign orientation resubscribe cleanup so development overlays do not lose the last provider/sample state during route stabilization or React effect re-entry; stale diagnostics are cleared only when live orientation is actually inactive/manual.
<autoloop-decisions-header version="1" block_seq="8" owner="implementer" phase_id="viewer-integration-and-verification" pair="implement" turn_seq="2" run_id="run-20260328T220654Z-04289b02" ts="2026-03-28T23:05:58.593282+00:00" />
Motion-only recovery retries must normalize `requestOrientationPermission()` results the same way as full startup: prompt `granted` and `unavailable` keep route orientation `unknown` until a real sample arrives or the no-provider timeout resolves.
The 500 ms provider selection window is only an arbitration grace period; if no provider has emitted yet, the coordinator must keep providers alive and let the first late sample claim readiness instead of tearing every provider down before the viewer timeout.
<autoloop-decisions-header version="1" block_seq="9" owner="test_author" phase_id="viewer-integration-and-verification" pair="test" turn_seq="1" run_id="run-20260328T220654Z-04289b02" ts="2026-03-28T23:08:58.623183+00:00" />
The viewer-shell regression guard for the cycle-2 motion-only retry fix now covers both branches of the normalized prompt result: it asserts the intermediate `unknown`/pending state after a successful prompt and a separate timeout-to-`denied` path when no provider emits.
For this repo layout, targeted unit validation should run from `SkyLensServerless/` with the app-local Vitest binary and `NODE_PATH=/workspace/SkyLens/SkyLensServerless/node_modules`; repo-root invocations still hit config/install-layout friction.
