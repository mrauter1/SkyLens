Fix viewer and alignment overlays on mobile: (1) open viewer overlay top is clipped and user cannot scroll back to top; (2) during alignment process overlay should hide so only target center and align button remain visible; (3) when AR is disabled and calibration not done, show clear buttons: one to enable camera and motion (only when not enabled) and one to align for first-time use; keep align button visible.
