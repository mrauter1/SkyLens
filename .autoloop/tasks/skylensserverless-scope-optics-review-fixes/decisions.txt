<autoloop-decisions-header version="1" block_seq="1" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260406T032910Z-976480d3" ts="2026-04-06T03:29:10.765401+00:00" />
Scope optics math must normalize inside exported helpers even when callers already normalized earlier, so direct helper calls cannot produce NaN and persisted-settings compatibility stays unchanged.
Desktop parity means aperture and magnification belong in the viewer quick-controls context on both mobile and desktop when scope mode is enabled; transparency and marker scale remain in Settings only.
Do not add a Zod parse for scopeRender unless implementation finds a broader untyped boundary than the current in-process metadata handoff, because the existing finite-number guard is the intended lightweight render-boundary validation.
<autoloop-decisions-header version="1" block_seq="2" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260406T033255Z-38f3ee02" ts="2026-04-06T03:32:56.011357+00:00" />
Desktop quick-control parity should be implemented in the existing desktop primary action row, not the expanded desktop overlay body, because that row is the desktop counterpart to the mobile quick-actions strip.
Malformed scopeRender handling should continue to rely on the existing in-process finite-number guard unless implementation uncovers a broader untyped boundary; add fallback/parity tests instead of schema churn by default.
<autoloop-decisions-header version="1" block_seq="3" owner="implementer" phase_id="apply-scope-optics-review-fixes" pair="implement" turn_seq="1" run_id="run-20260406T033255Z-38f3ee02" ts="2026-04-06T03:35:27.722357+00:00" />
Retained the existing finite-number `scopeRender` guard instead of adding Zod in `viewer-shell`, because the metadata is still produced in-process and helper-level tests now cover malformed fallback without adding render-path parse overhead.
Desktop scope quick controls were added as aperture and magnification sliders in the existing primary action row only when scope mode is enabled; the scope mode toggle remains in mobile quick actions and Settings, while transparency and marker scale remain Settings-only.
<autoloop-decisions-header version="1" block_seq="4" owner="implementer" phase_id="verify-scope-optics-review-fixes" pair="implement" turn_seq="1" run_id="run-20260406T033255Z-38f3ee02" ts="2026-04-06T03:51:35.613508+00:00" />
Verification for the scope-optics review fixes passed in both the targeted Vitest run and the full `pnpm test` sweep, so this run found no new regressions or pre-existing suite failures to classify.
