# Plan ↔ Plan Verifier Feedback

- 2026-03-27: Replaced the empty plan stub with a concrete two-phase implementation plan covering explicit alignment-state gating, live-view scroll locking, center-crosshair apply behavior, shared-model fallout control, and the exact unit-test surfaces that must be updated.
- PLAN-001 | blocking | `plan.md` milestone 1 and the interface section still allow the alignment panel to open from the existing settings-driven entrypoint (`"Align affordance or existing settings-driven alignment entrypoint"`), but the request says the alignment view should open only when the user presses Align. The current settings sheet has a separate `Alignment` button that calls `onFixAlignment`, so the plan currently permits an extra opener and leaves implementation/tests ambiguous. Minimal correction: explicitly require that any settings-sheet entry either stop opening the live alignment panel directly or be renamed/reworked into the same single Align entry semantics.
- PLAN-002 | blocking | `plan.md` says to lock `document.body` and `document.documentElement` scrolling `"only while the live AR viewer is mounted"`, and phase AC-2 repeats that broad live-usage contract. In the current code, the live route also renders blocked, permission-recovery, and fallback panels that may need scrolling on small screens. If scroll lock is applied to all live-route states, the plan would introduce an unrequested accessibility/compatibility regression. Minimal correction: narrow the scroll-lock trigger to active camera-stage / alignment-focus AR states and explicitly preserve scrollability for blocked or non-camera live screens.
- 2026-03-27: Revised the plan to resolve `PLAN-001` and `PLAN-002` by making the live fast-align panel a single-Align-entry contract and narrowing scroll lock to active camera-stage/alignment-focus AR states while preserving blocked/non-camera live-screen accessibility.
- PLAN-003 | non-blocking | Re-review found no remaining blocking issues. The revised plan now makes the fast-align panel a single-Align entry path, scopes scroll locking to active AR camera-stage/alignment-focus states, preserves blocked/non-camera live accessibility, and keeps the validation/rollback coverage aligned with those constraints.
