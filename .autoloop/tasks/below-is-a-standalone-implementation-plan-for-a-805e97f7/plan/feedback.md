# Plan ↔ Plan Verifier Feedback

- Replaced the empty planning artifacts with an implementation-ready plan aligned to the supplied sensor-overlay contract, including the intentional location-fallback change, viewer-owned Start AR flow, storage migration notes, and ordered regression controls.
- PLAN-001 `blocking`: The plan omits the request’s deployment contract from section 10. There is no explicit requirement for HTTPS/localhost, `Permissions-Policy` camera/geolocation/accelerometer/gyroscope/magnetometer delegation, or iframe `allow` handling anywhere in [plan.md](/workspace/SkyLens/.autoloop/tasks/below-is-a-standalone-implementation-plan-for-a-805e97f7/plan/plan.md) or [phase_plan.yaml](/workspace/SkyLens/.autoloop/tasks/below-is-a-standalone-implementation-plan-for-a-805e97f7/plan/phase_plan.yaml). That is an intent-fidelity and compatibility gap because the requested feature can fail even with correct app code when those headers/embed constraints are not planned. Minimal correction: add an explicit deployment/operational section plus a validation item/phase deliverable covering secure-context hosting, permissions-policy headers, and iframe delegation rules.
- PLAN-002 `blocking`: The calibration plan is behaviorally incomplete because it never specifies the required target-selection order from request section 5.2: Sun, then Moon, then brightest visible planet, then brightest visible star, then manual north marker fallback. Current plan text only promises align/reset/fine-adjust controls, so later implementation could pick any target or omit the north-marker fallback. Minimal correction: add the exact target-priority contract, note reuse of the existing astronomy pipeline, and include acceptance criteria for the no-suitable-object fallback path.
- PLAN-003 `blocking`: The browser-compatibility path is underspecified for iOS/WebKit. The request explicitly calls for changing the iOS permission helper to `DeviceOrientationEvent.requestPermission(true)` inside the Start AR activation path and for requesting motion/orientation permissions before camera startup. The current plan references a viewer-owned Start AR flow, but it never encodes the `requestPermission(true)` requirement or the supporting permission-helper changes in the interfaces, milestones, or phase acceptance criteria. Minimal correction: add this exact permission-helper contract and a compatibility validation item so the later implementation does not silently preserve the current weaker permission path in [lib/sensors/orientation.ts](/workspace/SkyLens/lib/sensors/orientation.ts).
- Producer revision cycle 2: addressed PLAN-001 through PLAN-003 by adding an explicit deployment/embedding contract, the calibration target-priority and manual north-marker fallback rules, and the required iOS/WebKit `DeviceOrientationEvent.requestPermission(true)` startup path to both the narrative plan and the phase acceptance criteria.
- PLAN-001 `non-blocking`: Resolved in cycle 2. The plan and phase contract now explicitly cover HTTPS/localhost hosting, `Permissions-Policy`, iframe `allow` delegation, secure-context gating, and deployment validation.
- PLAN-002 `non-blocking`: Resolved in cycle 2. The plan now fixes the calibration target-selection order to Sun, Moon, brightest visible planet, brightest visible star, then manual north marker, and threads that contract into phase acceptance criteria.
- PLAN-003 `non-blocking`: Resolved in cycle 2. The plan now explicitly encodes the iOS/WebKit `DeviceOrientationEvent.requestPermission(true)` helper path, startup ordering, and compatibility validation.
