# Plan ↔ Plan Verifier Feedback

- Added an implementation-ready plan and 3 ordered phases after tracing the actual server-side surface (`/api/config`, `/api/tle`, `/api/health`, and static-safety for `/view`); the plan now reflects the approved assumption that replacement browser-safe live-data plumbing is allowed but static live-data degradation is not.
- PLAN-001 `blocking`: The plan allows browser-safe replacement live-data sources or a hosted proxy/service, but it does not explicitly account for the existing user-visible privacy/data-flow promises that are tied to the current providers. Evidence: `plan.md` "Clarified Assumptions" and "Target Architecture" permit alternate providers/proxies, while current app copy in `lib/config.ts` says approximate aircraft queries go directly from the browser to OpenSky. If the fork uses a hosted proxy or non-OpenSky provider, the shipped copy can become false or the data-flow behavior can change silently, which violates the parity/safety requirement. Minimal correction: add an explicit compatibility constraint that either preserves the current browser-direct provider behavior where privacy copy is provider-specific, or updates the affected copy, tests, and parity documentation whenever the chosen plumbing changes the data path or privacy contract.
- PLAN-002 `non-blocking`: `phase_plan.yaml` phase `replace-server-runtime-surfaces` uses AC-4 as a permission statement ("The fork is permitted to use...") rather than a verifiable implementation outcome. Replace it with a concrete acceptance criterion, for example that the selected live-data provider/proxy is documented and validated against the preserved client contracts and privacy constraints.
- Updated cycle 2 plan to treat privacy/data-flow reassurance copy as a preserved compatibility surface, require provider/proxy-path review before closing migration, and replace the non-verifiable phase acceptance criterion with a concrete outcome tied to truthful copy and tests.
- PLAN-003 `non-blocking`: Re-verification complete. `PLAN-001` is resolved by the new compatibility, validation, and risk language for provider/proxy-driven privacy/data-flow copy, and `PLAN-002` is resolved by replacing the permission-style acceptance criterion with a verifiable provider-path outcome. No remaining blocking findings.
