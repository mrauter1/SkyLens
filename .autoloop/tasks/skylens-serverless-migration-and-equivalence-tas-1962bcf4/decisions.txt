<autoloop-decisions-header version="1" block_seq="1" owner="runtime" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260328T152027Z-2ab1e610" ts="2026-03-28T15:23:17.353110+00:00" entry="questions" qa_seq="1" />
WARNING: a fully serverless/browser-only SkyLens fork is likely not able to preserve the current live-data behavior with the existing upstreams. I verified that the current TLE source (`celestrak.org`) and aircraft source (`opensky-network.org`) do not appear to return browser CORS headers, so direct client-side fetches are likely blocked. 1. Should the serverless fork be allowed to use different browser-accessible live data sources or a third-party hosted proxy/service, as long as the user-visible behavior stays equivalent? Answer YES or NO. Best supposition: YES, because otherwise preserving live satellite/TLE behavior without any server component is likely infeasible. 2. If the answer to 1 is NO, should I instead plan for a serverless fork that preserves UI/UX and demo/offline behavior but intentionally degrades live satellite freshness by bundling static catalog data at build time? Answer YES or NO. Best supposition: NO, because that would violate the stated no-loss-capability requirement unless you explicitly approve the behavior break.
Best supposition: Allow the serverless fork to use browser-accessible replacement live-data sources or a hosted proxy/service so feature parity can remain intact without editing the root app.
<autoloop-decisions-header version="1" block_seq="2" owner="runtime" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260328T152027Z-2ab1e610" ts="2026-03-28T15:23:17.353490+00:00" entry="answers" qa_seq="1" source="auto" />
I’m checking the task snapshot and the chronological log to see whether this constraint has already been clarified in-repo, then I’ll answer with the safest explicit assumption tied to the stated requirements.

The task text is explicit about parity and silent on matching the exact upstream providers. I’m checking whether the repo defines “fully serverless” in a way that would forbid a third-party browser-safe source or hosted proxy before I lock the binary answers.

I found an existing product doc that explicitly rejects a server-side OpenSky proxy for the main app, so I’m reading those lines to see whether they constrain this fork or just the current implementation.

1. YES
2. NO

Safest explicit assumption: allow the serverless fork to use browser-accessible replacement live-data sources or a third-party hosted proxy/service.

Reasoning from repo requirements: the request snapshot requires “no loss of capability versus the original app” and “equivalent UI and behavior,” while the raw log for this run already records that the current CelesTrak and OpenSky upstreams do not appear browser-CORS-accessible. Under those constraints, forbidding alternate live-data plumbing would make parity likely infeasible. By contrast, bundling static catalog data at build time would knowingly reduce live-data freshness and therefore conflicts with the stated parity/no-loss requirement unless a human explicitly approves that degradation.
<autoloop-decisions-header version="1" block_seq="3" owner="planner" phase_id="task-global" pair="plan" turn_seq="2" run_id="run-20260328T152027Z-2ab1e610" ts="2026-03-28T15:23:17.426559+00:00" />
SkyLensServerless may use browser-accessible replacement live-data sources or a third-party hosted proxy/service when needed to preserve capability and behavior; exact upstream parity is not required.
Bundling static live satellite data as a permanent replacement is not allowed without a new explicit clarification because it would reduce live-data capability.
The serverless fork should be a standalone copied app under SkyLensServerless with no root-app file edits beyond autoloop artifacts.
The fork must preserve the permissions-policy/embed contract through fork-local hosting or app configuration rather than root configuration.
<autoloop-decisions-header version="1" block_seq="4" owner="planner" phase_id="task-global" pair="plan" turn_seq="3" run_id="run-20260328T152027Z-2ab1e610" ts="2026-03-28T15:26:03.168993+00:00" />
Privacy/data-flow reassurance copy is part of the fork's compatibility contract; if a hosted proxy or alternate provider changes where requests go, the copy, tests, and parity documentation must be updated so the fork does not ship false OpenSky-specific assurances.
The selected live-data provider/proxy path must be documented in the plan and verified against both payload-contract parity and user-visible privacy/data-flow messaging.
<autoloop-decisions-header version="1" block_seq="5" owner="implementer" phase_id="bootstrap-standalone-fork" pair="implement" turn_seq="1" run_id="run-20260328T152027Z-2ab1e610" ts="2026-03-28T15:27:39.723241+00:00" />
Bootstrap created `SkyLensServerless/` as a fork-local copy of the current app/runtime/test surface (`app`, `components`, `lib`, `public`, `tests`, and core config files) so all existing relative imports resolve inside the fork without root-app runtime dependencies.
The fork uses `skylens-serverless` package identity, a namespaced viewer settings storage key, and Playwright port `3100` to avoid collisions with the root app during parallel local development and testing.
`SkyLensServerless/next.config.ts` explicitly sets `turbopack.root` to the fork directory so Next stops inferring the parent repository as the workspace root when both projects have lockfiles.
<autoloop-decisions-header version="1" block_seq="6" owner="test_author" phase_id="bootstrap-standalone-fork" pair="test" turn_seq="1" run_id="run-20260328T152027Z-2ab1e610" ts="2026-03-28T15:34:30.775108+00:00" />
Bootstrap-phase regression coverage includes a fork-contract meta-test that asserts package/config isolation (`package.json`, Playwright port, `turbopack.root`, viewer settings storage key) and verifies that relative imports from `SkyLensServerless/app`, `components`, and `lib` do not resolve outside the fork root.
<autoloop-decisions-header version="1" block_seq="7" owner="implementer" phase_id="replace-server-runtime-surfaces" pair="implement" turn_seq="1" run_id="run-20260328T152027Z-2ab1e610" ts="2026-03-28T15:37:33.836591+00:00" />
SkyLensServerless now removes fork-local `app/api/config`, `app/api/health`, and `app/api/tle`; config and health are derived in-process, and `/view` reads query state through a Suspense-wrapped client child so the route stays statically prerenderable.
The serverless TLE path now uses a namespaced browser cache in `localStorage` with the existing 6-hour refresh and 24-hour stale windows, and it fetches CelesTrak through `NEXT_PUBLIC_SKYLENS_TLE_PROXY_URL_TEMPLATE` (default `https://corsproxy.io/?{url}`) to preserve `TleApiResponse` semantics without a fork-owned server.
<autoloop-decisions-header version="1" block_seq="8" owner="test_author" phase_id="replace-server-runtime-surfaces" pair="test" turn_seq="1" run_id="run-20260328T152027Z-2ab1e610" ts="2026-03-28T15:46:21.963860+00:00" />
Serverless config-contract coverage now asserts `NEXT_PUBLIC_SKYLENS_BUILD_VERSION` precedence over `SKYLENS_BUILD_VERSION` so future changes do not silently break the client-visible build version after removing `/api/config`.
<autoloop-decisions-header version="1" block_seq="9" owner="implementer" phase_id="verify-parity-and-document" pair="implement" turn_seq="1" run_id="run-20260328T152027Z-2ab1e610" ts="2026-03-28T15:48:38.103968+00:00" />
Because SkyLensServerless fetches live TLE data through a browser-safe relay instead of a fork-owned route, the fork's reassurance copy must explicitly disclose the satellite relay path to keep the user-visible privacy/data-flow contract truthful.
Phase parity documentation lives in `SkyLensServerless/PARITY.md` and treats each removed root server surface (`/api/config`, `/api/tle`, `/api/health`, and server-side `/view` parsing) as a compatibility surface with a paired validation reference.
<autoloop-decisions-header version="1" block_seq="10" owner="test_author" phase_id="verify-parity-and-document" pair="test" turn_seq="1" run_id="run-20260328T152027Z-2ab1e610" ts="2026-03-28T15:57:50.243002+00:00" />
Verify-parity e2e coverage intentionally targets a deterministic visible demo star label (`Polaris`) for detail-card interaction instead of a mobile-layout-dependent aircraft label so the test continues to assert real object-selection behavior without encoding label-layout flake.
Verify-parity strategy treats the satellite relay disclosure as a compatibility surface and maps it to both unit and e2e assertions so future provider-path changes cannot silently drift away from truthful user-visible copy.
