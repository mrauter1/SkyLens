# Test Author ↔ Test Auditor Feedback

- Task ID: skylensserverless-desktop-declutter-mobile-navig-bc222291
- Pair: test
- Phase ID: viewer-shell-declutter-and-mobile-nav
- Phase Directory Key: viewer-shell-declutter-and-mobile-nav
- Phase Title: Desktop declutter, hover parity, and mobile navigation hardening
- Scope: phase-local authoritative verifier artifact

- Added unit coverage for the desktop-only warning-stack breakpoint contract in `SkyLensServerless/tests/unit/viewer-shell.test.ts`, alongside the existing desktop panel, hover persistence, manual-pan align-disable, and mobile overlay/alignment regression tests.
- Recorded the behavior-to-test map and the current Playwright environment gap in `test_strategy.md` so auditors can distinguish covered regressions from infrastructure-blocked e2e execution.
- TST-000 | non-blocking | No blocking audit findings in phase scope. The added warning-stack breakpoint assertion complements the existing viewer-shell regression set, and the documented coverage map accounts for both the desktop declutter changes and the preserved mobile navigation behaviors. The remaining Playwright gap is environmental in this container, not a coverage-design defect.
- Cycle 2 producer confirmation: reran the three phase-local unit assertions for desktop compact shell, desktop hover summary persistence, and manual-pan desktop Align disable; all passed locally in `SkyLensServerless/`. A broader full-file rerun still shows unrelated startup-gating failures elsewhere in `viewer-shell.test.ts`, so this phase keeps relying on scoped validation plus the existing e2e desktop-shell coverage.
- TST-001 | blocking | The current phase validation is too narrow for the changed `viewer-shell` surface. In `SkyLensServerless/`, `./node_modules/.bin/vitest run tests/unit/viewer-shell.test.ts --reporter=verbose` still fails adjacent tests in the same file, including `keeps the viewer privacy reassurance copy visible before live startup begins`, `shows the secure-context failure screen when live AR is unavailable on this origin`, `supports keyboard panning in manual mode for desktop fallback`, and `keeps motion-only retries pending until a usable sample arrives`. Those expectations still read desktop-visible copy and status directly from `container.textContent` even though this phase intentionally moved much of that desktop content behind the compact `Open viewer` shell. That leaves preserved behavior and failure-path coverage for the changed desktop contract effectively unaudited. Minimal correction: update the affected desktop viewer-shell tests to exercise the compact shell explicitly, such as opening the desktop viewer panel before asserting panel-only copy or status, then rerun the relevant viewer-shell regression slice instead of relying on only the three newly added assertions.
- Cycle 3 producer update: adjusted the adjacent desktop-path unit tests in `SkyLensServerless/tests/unit/viewer-shell.test.ts` to open the compact desktop viewer panel before asserting panel-only copy, status, or actions; also updated the affected desktop status assertions to match the current compact panel text contract. Focused reruns covering the verifier’s blocking cases now pass locally, including the desktop blocked-start panel, desktop motion-pending state, manual-observer fallback display, relative/absolute sensor status rows, and the previously added declutter / hover / manual-pan Align tests. One separate non-camera fallback test still times out locally and remains called out as a known gap rather than a resolved cycle-3 desktop-shell fix.
- TST-002 | blocking | TST-001’s desktop-shell expectation drift is largely fixed, but the phase test story is still not reliable enough to close. Evidence: `keeps location and orientation startup live in verified non-camera fallback` continues to time out even when targeted by itself, and `reuses the combined recovery CTA inside the motion panel when camera and motion are both missing` still times out in broader grouped reruns after passing in a tighter focused run. Those are adjacent startup/recovery paths on the same `viewer-shell` surface, so the current regression signal remains order-dependent and incomplete. Minimal correction: stabilize or refactor those two tests so they do not depend on unresolved async startup/recovery timing, then rerun the relevant grouped `viewer-shell` slice and confirm it stays green outside isolated one-off executions.
- Cycle 4 producer update: stabilized the two remaining startup/recovery regressions in `SkyLensServerless/tests/unit/viewer-shell.test.ts` by stubbing `requestAnimationFrame` in the non-camera and combined camera+motion recovery cases, driving combined recovery with an explicit emitted orientation sample, and cleaning up the live session before teardown. The grouped verifier slice now passes locally: `20` relevant viewer-shell tests covering desktop declutter, hover parity, manual-pan align disable, startup blocker/recovery states, and adjacent startup/recovery regressions all passed together under `./node_modules/.bin/vitest run tests/unit/viewer-shell.test.ts -t \"(blocks live startup behind a secure context requirement|requests motion, then camera, then location when Start AR is pressed in-view|keeps route orientation unknown until the first usable sample arrives|times out unknown startup to denied when orientation APIs exist but no provider emits|times out unknown startup to unavailable when no orientation APIs exist|hydrates a persisted manual observer without re-requesting location on denied deep links|shows the startup-blocked viewer panel before live startup begins|supports keyboard panning in manual mode for desktop fallback|recenters manual mode only after a second tap lands within the double-tap window|keeps motion-only retries pending until a usable sample arrives|clears stale live sensor state when switching into demo mode|surfaces the relative-sensor warning state and calibration target guidance|distinguishes absolute-sensor mode from relative and manual fallbacks|renders development diagnostics from the selected provider state|keeps desktop chrome compact until the viewer panel is explicitly opened|uses hover for desktop summary focus without clearing explicit selection details|disables the desktop Align action when manual pan is active|reuses the combined recovery CTA inside the motion panel when camera and motion are both missing|keeps camera recovery active when combined motion retry throws and surfaces the motion retry error|keeps location and orientation startup live in verified non-camera fallback)\" --reporter=verbose`.
- TST-003 | non-blocking | No blocking audit findings remain in phase scope. TST-001 and TST-002 are resolved by the cycle-4 panel-contract and startup/recovery stabilization work, and the documented grouped viewer-shell slice now provides deterministic regression coverage for the changed desktop/mobile behavior. The only remaining gap is environmental Playwright execution in this container, not a defect in the phase-local test design.


## System Warning (cycle 1)
No promise tag found, defaulted to <promise>INCOMPLETE</promise>.
