<autoloop-decisions-header version="1" block_seq="1" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260329T004955Z-52e78c54" ts="2026-03-29T00:49:55.527276+00:00" />
Remove SkyLensServerless TLE relay/proxy-template support and fetch CelesTrak directly in the browser; update copy and tests to treat direct upstream access as the only supported path.
Desktop declutter should mirror the existing mobile quick-action model by hiding verbose viewer details behind an explicit viewer panel instead of keeping the current desktop content stack always open.
Desktop hover should reuse the existing active/selected object detail pipeline and must not change mobile crosshair center-lock semantics or manual-pan behavior.
Mobile navigation fixes should stay focused on viewer overlay, alignment, and settings state transitions inside `components/viewer/viewer-shell.tsx` unless implementation proves a separate route-level bug exists.
<autoloop-decisions-header version="1" block_seq="2" owner="planner" phase_id="task-global" pair="plan" turn_seq="2" run_id="run-20260329T004955Z-52e78c54" ts="2026-03-29T00:53:41.482602+00:00" />
Superseding the prior plan direction: this task removes `corsproxy.io` usage and makes direct CelesTrak requests the default path, but it does not remove optional non-`corsproxy.io` relay-template compatibility without explicit user confirmation.
Desktop control planning should treat `Motion` as an explicit desktop control slot, not only passive status text, while still reusing the existing permission-recovery helpers and labels.
<autoloop-decisions-header version="1" block_seq="3" owner="implementer" phase_id="direct-tle-api" pair="implement" turn_seq="1" run_id="run-20260329T004955Z-52e78c54" ts="2026-03-29T00:56:06.395528+00:00" />
Applied a nullability-only `viewer-shell.tsx` fix to unblock `next build` and regenerate `SkyLensServerless/out`; no startup behavior change was intended because the existing `orientation === null` branch already returned before the affected call.
<autoloop-decisions-header version="1" block_seq="4" owner="implementer" phase_id="viewer-shell-declutter-and-mobile-nav" pair="implement" turn_seq="1" run_id="run-20260329T004955Z-52e78c54" ts="2026-03-29T01:05:56.911262+00:00" />
Desktop warnings now surface through a compact top-of-screen banner stack, while the full desktop viewer content stays behind an explicit viewer panel toggle to keep the default shell aligned with the mobile action density.
Desktop hover updates the shared active-summary pipeline and can populate the desktop detail surface when nothing is selected, but explicit click selection remains the source of truth for the persistent detail panel until hover leaves or selection is cleared.
The compact top warning stack is desktop-only; mobile keeps warning copy inside the existing overlay/navigation surfaces to avoid adding persistent top chrome outside the mobile contract.
Desktop Align remains a visible control slot, but it is disabled with a motion-required status whenever manual-pan or another non-openable state would otherwise leave the action without a reachable alignment panel.
<autoloop-decisions-header version="1" block_seq="5" owner="test_author" phase_id="viewer-shell-declutter-and-mobile-nav" pair="test" turn_seq="1" run_id="run-20260329T004955Z-52e78c54" ts="2026-03-29T01:27:03.185723+00:00" />
Desktop-only warning-stack coverage is asserted at the unit layer via the `hidden sm:flex` breakpoint contract because the current container cannot run Playwright browser coverage without additional Chromium runtime libraries.
<autoloop-decisions-header version="1" block_seq="6" owner="test_author" phase_id="viewer-shell-declutter-and-mobile-nav" pair="test" turn_seq="2" run_id="run-20260329T004955Z-52e78c54" ts="2026-03-29T01:29:30.631954+00:00" />
Cycle-2 validation for this phase is intentionally scoped to the three desktop declutter/mobile-hardening assertions in `SkyLensServerless/tests/unit/viewer-shell.test.ts`, because the full file currently has unrelated startup-gating failures that would otherwise obscure the changed-surface signal.
<autoloop-decisions-header version="1" block_seq="7" owner="test_author" phase_id="viewer-shell-declutter-and-mobile-nav" pair="test" turn_seq="3" run_id="run-20260329T004955Z-52e78c54" ts="2026-03-29T01:33:55.576886+00:00" />
Desktop `viewer-shell` tests that assert panel-only copy, status, or recovery controls must explicitly open `desktop-open-viewer-action`; the compact shell no longer leaves those details visible by default.
Desktop panel status assertions should match the rendered compact-shell text contract (`Location Manual observer`, `Motion Pending`, `Sensor Absolute`, etc.) rather than the older colon-delimited string shape from the pre-declutter stack.
Pre-start live desktop coverage now treats the compact blocker panel as the authoritative desktop surface before startup begins; privacy-reassurance copy remains covered in the open desktop demo panel and mobile overlay tests rather than the blocked-start desktop state.
<autoloop-decisions-header version="1" block_seq="8" owner="test_author" phase_id="viewer-shell-declutter-and-mobile-nav" pair="test" turn_seq="4" run_id="run-20260329T004955Z-52e78c54" ts="2026-03-29T01:43:21.133197+00:00" />
The non-camera live fallback and combined camera+motion recovery tests stub `requestAnimationFrame` because those assertions only care about permission/state transitions, and the live render loop otherwise introduces unrelated teardown churn in JSDOM.
Combined camera+motion recovery is stabilized by asserting the intermediate `orientation: unknown` route transition, then emitting an explicit usable orientation sample to drive the final `granted` route state deterministically.
