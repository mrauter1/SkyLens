# Plan ↔ Plan Verifier Feedback

- Replaced the empty plan with a two-phase implementation plan that locks scope to `SkyLensServerless/`, makes the direct CelesTrak fetch path explicit, and targets desktop declutter/mobile-navigation work in `viewer-shell.tsx` with concrete regression controls.
- PLAN-001 | blocking | `plan.md` compatibility notes and milestone 1, plus the new entry in `decisions.txt`, require removing `NEXT_PUBLIC_SKYLENS_TLE_PROXY_URL_TEMPLATE` support entirely. That is a public configuration regression that the request did not explicitly authorize: the user required removing `corsproxy.io` usage and switching SkyLensServerless to direct API calls, but did not confirm deleting all configurable relay support. A deployment using a non-`corsproxy.io` relay would silently lose a working path. Minimal correction: revise the plan to remove only `corsproxy.io` usage and make direct upstream fetches the default implementation, or explicitly obtain user confirmation before planning removal of relay-template support.
- PLAN-002 | non-blocking | `plan.md` milestone 2 says desktop will expose `Open viewer`, a context-sensitive permission CTA, a `Motion` status/warning affordance, and `Align`. The request names `Open Viewer`, `Enable Camera`, `Motion`, and `Align` as prominent desktop controls. Clarify whether `Motion` remains an explicit action/control on desktop or is intentionally represented by status plus the shared permission CTA so implementation does not underdeliver that control requirement.
- Revised the plan to address PLAN-001 by making direct CelesTrak fetches the default path without silently removing optional non-`corsproxy.io` relay-template compatibility, and clarified PLAN-002 by treating `Motion` as an explicit desktop control slot that reuses the existing permission/status helpers.
- Verification cycle 2: no additional findings. PLAN-001 is resolved by the default-vs-optional-override compatibility rewrite, and PLAN-002 is resolved by the explicit desktop control-slot wording in both `plan.md` and `phase_plan.yaml`.
