<autoloop-decisions-header version="1" block_seq="1" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260406T134900Z-90d728e0" ts="2026-04-06T13:49:01.143436+00:00" />
Treat `scopeOptics.magnificationX` as the only canonical scope zoom input; keep `scope.verticalFovDeg` as a derived compatibility field for persistence/runtime call sites until implementation removes direct dependence on it.
Legacy persisted `scope.verticalFovDeg` must seed canonical magnification only when `scopeOptics.magnificationX` is absent, using the inverse of the same deterministic magnification-to-FOV conversion so migration stays stable across reloads.
Deep-star scope optics filtering is additive after existing daylight/likely-visible suppression and horizon checks; this task must not change the current suppression order or thresholds.
<autoloop-decisions-header version="1" block_seq="2" owner="implementer" phase_id="canonical-scope-optics-and-compatibility" pair="implement" turn_seq="1" run_id="run-20260406T134900Z-90d728e0" ts="2026-04-06T13:53:26.732918+00:00" />
Use a shared virtual apparent-field default of `500` in the magnification↔scope-FOV helpers so the canonical default `50x` optics keep the existing `10°` compatibility FOV while runtime zoom semantics move entirely onto `scopeOptics.magnificationX`.
Normalize `scope.verticalFovDeg` as a write-time/read-time compatibility field derived from canonical magnification; the settings sheet no longer owns direct scope-FOV edits.
<autoloop-decisions-header version="1" block_seq="3" owner="test_author" phase_id="canonical-scope-optics-and-compatibility" pair="test" turn_seq="1" run_id="run-20260406T134900Z-90d728e0" ts="2026-04-06T14:04:51.770143+00:00" />
Cover the write path explicitly so tests fail if persisted `scope.verticalFovDeg` stops being regenerated from canonical `scopeOptics.magnificationX` and stale legacy FOV values begin leaking back into saved settings.
<autoloop-decisions-header version="1" block_seq="4" owner="implementer" phase_id="unify-scope-runtime-and-deep-star-optics" pair="implement" turn_seq="1" run_id="run-20260406T134900Z-90d728e0" ts="2026-04-06T14:07:20.927539+00:00" />
Deep-star runtime objects now emit `metadata.scopeRender` and `scopeFilter.effectiveLimitMag` using the same optics semantics as the bright-star path, while the scope canvas consumes only profile-driven `intensity`, `corePx`, and `haloPx`.
Scope canvas rendering uses compact two-pass halo/core draws from the optics profile; magnification changes deep-star spacing through projection/FOV, not through linear diameter growth.
<autoloop-decisions-header version="1" block_seq="5" owner="test_author" phase_id="unify-scope-runtime-and-deep-star-optics" pair="test" turn_seq="1" run_id="run-20260406T134900Z-90d728e0" ts="2026-04-06T14:17:39.069537+00:00" />
Validate scope canvas rendering by mocking the 2D context and asserting recorded halo/core draw calls, because jsdom has no real canvas implementation and the contract change is about profile-driven radii, opacity, and color semantics rather than pixel snapshots.
