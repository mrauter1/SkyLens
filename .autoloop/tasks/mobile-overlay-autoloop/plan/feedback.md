# Plan ↔ Plan Verifier Feedback
- Added a single-phase implementation plan because the requested UI adjustment is one coherent viewer-shell slice, not a multi-milestone feature.
- Locked scope to the mobile informational overlay presentation, preserved desktop behavior and viewer contracts, and called out iOS safe-area/stage-interaction regressions plus the unit/full-test validation needed to prevent them.
- PLAN-001 [blocking]: The plan silently narrows the user request from "overlays are collapsed into a small bottom button" to only the "informational overlay stack" and then records that narrower interpretation in `decisions.txt` ("do not collapse ... the settings entry point"). In the current viewer shell, the top header/title badge and settings control remain visible on mobile outside the bottom sheet, so this is a real product-behavior choice, not a harmless wording tweak. Without an authoritative clarification, later turns could ship a mobile UI that still shows top-of-screen overlays even if the request intended all non-stage overlays to collapse. Minimal correction: either ask for clarification on whether the mobile header/settings chrome should also collapse into the bottom affordance, or revise the plan to cover all mobile overlay chrome and explicitly justify any retained always-visible elements from confirmed intent.
- PLAN-002 [non-blocking]: The validation section asks for mobile collapsed/expanded assertions, but the current viewer tests rely heavily on `container.textContent` while desktop-only content remains mounted in the DOM behind responsive classes. That means naive text assertions can pass without actually proving the mobile sheet is collapsed or that desktop behavior stayed isolated. Minimal correction: call for element-level assertions on the mobile trigger/sheet controls, and add stable selectors or accessible labels if needed so the tests validate behavior rather than duplicated hidden text.
