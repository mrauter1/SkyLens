# Plan ↔ Plan Verifier Feedback

- 2026-03-28: Replaced the placeholder plan with a single-phase implementation plan centered on the existing `ViewerShell` state booleans, because the bug is a local transition/render issue rather than a broader architecture problem.
- 2026-03-28: Documented the current regression source explicitly: `openAlignmentExperience()` currently opens the alignment panel and mobile focus together, so later implementation should introduce an explicit `Start alignment` transition and assert the panel is hidden during focus mode.
- PLAN-001 blocking: `plan.md` leaves the required `Start alignment` control ambiguous in the no-live-sample path. Evidence: the request explicitly requires "On the align view, provide/keep a clear Start alignment action," but `plan.md` Risk Register R4 says the start action may be "disabled or withheld accordingly." Allowing it to be withheld would let implementation regress to a guidance-only panel with no explicit start control. Minimal correction: require the mobile align view to keep a visible `Start alignment` CTA whenever the panel is open, using a disabled state plus explanatory copy when calibration cannot run yet.
- PLAN-002 blocking: `phase_plan.yaml` does not encode the user-requested broader validation closeout. Evidence: the request requires updating tests and running project tests/lint/types as needed, and `plan.md` mentions broader validation, but the only machine-readable phase scope, acceptance criteria, and deliverables cover focused viewer-shell tests. Minimal correction: add explicit phase-plan scope and acceptance criteria for the required lint/type/test validation, or add a final validation phase if that is clearer.
- 2026-03-28: Addressed PLAN-001 by making the plan require a visible `Start alignment` CTA on the mobile align view even before calibration can run, with a disabled state plus explanatory guidance rather than withholding the action.
- 2026-03-28: Addressed PLAN-002 by extending the single-phase contract to include the requested test/lint/type validation in phase scope, acceptance criteria, deliverables, and risk coverage so the machine-readable plan matches the narrative plan and request.
- PLAN-003 non-blocking: No blocking verifier issues remain after the second pass. The revised plan now keeps `Start alignment` visible on the align view even when calibration is unavailable, aligns the single-phase contract with the requested test/lint/type validation, preserves the mobile two-step focus/panel split recorded in `decisions.txt`, and remains a small local change centered on `ViewerShell` plus focused viewer-shell tests.
