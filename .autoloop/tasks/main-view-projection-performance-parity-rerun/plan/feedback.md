# Plan ↔ Plan Verifier Feedback
- 2026-04-08: Replaced the placeholder plan with an implementation-ready sequence covering shared non-scope projection context, hot-path constellation validation removal, main-view deep-star settings/governor/diagnostics, B-V color parity, and deterministic regression coverage because the current code already has partial optics support but still splits projection inputs and lacks workload governance.
- PLAN-001 `blocking`: [plan.md Milestone 2, plan.md Validation, phase_plan.yaml deep-star-governance-and-settings] The plan never converts "Keep conservative startup optics baseline" plus the acceptance requirement for a "Conservative startup visible-star count band documented and enforced in tests" into a concrete contract. It mentions a conservative startup baseline, but it does not specify the owning state/constants, the visible-star count band to preserve, or where that band will be documented/tested. Failure mode: implementation keeps or changes startup optics arbitrarily, and later phases cannot verify intent fidelity against a deterministic startup workload target. Minimal correction: pin the startup-baseline owner (for example main-view optics defaults and/or initial governor tier), require a documented visible-star count band, and add explicit tests for that band.
- PLAN-002 `blocking`: [plan.md Milestone 2, phase_plan.yaml deep-star-governance-and-settings] The governor precedence contract is left partially open-ended by "unless task validation reveals an existing stronger invariant," and the plan does not fully lock the decision order between hard-off gates, focus/selection exceptions, tier transitions, and diagnostics reporting. Failure mode: different implementations can legally choose different precedence behaviors, causing silent regressions in when deep stars render or which transition reason is surfaced, despite the task requiring deterministic thresholds/hysteresis/precedence coverage. Minimal correction: replace the hedge with one explicit precedence order and require diagnostics/tests to assert that same order.
- 2026-04-08: Tightened the plan after verifier feedback by pinning the startup-baseline owner to `getDefaultMainViewOptics()` plus the initial governor tier, requiring a documented numeric startup visible-deep-star count band in tests, and replacing the hedged precedence note with one explicit rule order that diagnostics and tests must assert.
- PLAN-001 `non-blocking`: Re-verified. The revised plan now pins the startup-baseline owner to `getDefaultMainViewOptics()` plus the governor’s initial tier and requires a documented numeric startup visible-deep-star count band in tests, resolving the earlier blocking gap.
- PLAN-002 `non-blocking`: Re-verified. The revised plan now fixes one explicit precedence order and carries that same order into the phase contract, diagnostics expectations, and deterministic test coverage, resolving the earlier blocking gap.
