<autoloop-decisions-header version="1" block_seq="1" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260408T021240Z-fdc4b072" ts="2026-04-08T02:12:40.741352+00:00" />
Non-scope visual parity should be enforced by one shared stage projection context in viewer-shell rather than by duplicating projection call arguments across markers, deep stars, constellation segments, and focused trails.
Bundled constellation catalog validation should still fail loudly, but it must move out of the per-frame production build path into one-time or cached initialization.
Main-view deep-star governance should layer on top of existing daylight/layer/observer gates and must not change scope-mode rendering behavior.
<autoloop-decisions-header version="1" block_seq="2" owner="planner" phase_id="task-global" pair="plan" turn_seq="2" run_id="run-20260408T021240Z-fdc4b072" ts="2026-04-08T02:17:04.285131+00:00" />
The startup deep-star workload contract is owned by getDefaultMainViewOptics() together with the governor's initial tier, and later turns must preserve a documented numeric startup visible-deep-star count band in tests.
Governor precedence is fixed: hard-off gates first, tier selection second, center-lock/selected marker-label exceptions third, diagnostics reason reporting last.
<autoloop-decisions-header version="1" block_seq="3" owner="implementer" phase_id="projection-context-and-hot-path" pair="implement" turn_seq="1" run_id="run-20260408T021240Z-fdc4b072" ts="2026-04-08T02:19:06.968411+00:00" />
Non-scope stage projection now always materializes explicit sourceWidth/sourceHeight values in viewer-shell, falling back to viewport dimensions before dispatching shared projection calls so aligned consumers cannot diverge on omitted camera source metadata.
Bundled constellation validation is now owned by constellations module initialization against the bundled star catalog; buildVisibleConstellations no longer revalidates per caller invocation.
<autoloop-decisions-header version="1" block_seq="4" owner="test_author" phase_id="projection-context-and-hot-path" pair="test" turn_seq="1" run_id="run-20260408T021240Z-fdc4b072" ts="2026-04-08T02:27:15.467816+00:00" />
Phase test coverage anchors non-scope parity with four deterministic checks: default-scale constellation endpoint alignment, magnified constellation endpoint alignment, magnified focused-trail alignment, and magnified deep-star alignment in the existing scope-runtime suite.
<autoloop-decisions-header version="1" block_seq="5" owner="implementer" phase_id="deep-star-governance-and-settings" pair="implement" turn_seq="1" run_id="run-20260408T021240Z-fdc4b072" ts="2026-04-08T02:29:56.509189+00:00" />
Main-view deep-star governance now uses four deterministic tiers keyed only by main-view magnification with hysteresis (`baseline`/`mag6p5`, `standard`/`mag8p0`, `detailed`/`mag9p5`, `precision`/`mag10p5`) so startup remains anchored to `getDefaultMainViewOptics()` plus the `baseline` tier.
Main-view hard-off precedence is implemented exactly as planned: observer missing, stars layer disabled, daylight suppression, then persisted main-view deep-stars toggle off; diagnostics mirror the winning hard-off source or the last governor transition reason.
Main-view deep stars continue to influence center-lock and selected-detail behavior, but non-focused main-view deep stars no longer emit markers or labels; only center-locked or selected deep stars surface those overlays.
The shared B-V palette now lives in `lib/viewer/star-colors.ts`, and focused main-view deep-star markers keep the amber focus halo while reusing the same B-V fill semantics as the scope star canvas.
<autoloop-decisions-header version="1" block_seq="6" owner="test_author" phase_id="deep-star-governance-and-settings" pair="test" turn_seq="1" run_id="run-20260408T021240Z-fdc4b072" ts="2026-04-08T02:42:20.544000+00:00" />
Phase test coverage now locks the preserved invariant that `mainViewDeepStarsEnabled` only gates main-view deep-star work; scope-mode deep-star canvas output must remain active when scope mode is on and the new main-view toggle is off.
The startup visible-deep-star band remains enforced with a deterministic synthetic magnitude fixture in `scope-optics.test.ts`, while runtime workload-cut assertions use zero fetch requests rather than DOM absence alone to catch hidden background work regressions.
<autoloop-decisions-header version="1" block_seq="7" owner="test_author" phase_id="color-parity-and-regression-validation" pair="test" turn_seq="1" run_id="run-20260408T021240Z-fdc4b072" ts="2026-04-08T02:49:26.110956+00:00" />
Final phase test ownership is split deliberately: `scope-star-canvas.test.tsx` anchors the canonical B-V band outputs, `viewer-shell-scope-runtime.test.tsx` guards non-scope focused-marker parity, `viewer-settings.test.tsx` locks legacy-safe storage reads, and `viewer-shell.test.ts` owns the settings-sheet bridge persistence assertion.
