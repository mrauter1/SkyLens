# Plan ↔ Plan Verifier Feedback

- Added the first implementation-ready plan: one coherent phase centered on `viewer-shell.tsx`, with explicit state/interface constraints, regression controls, and validation focused on opt-in AR startup, always-visible toggles, disable teardown, and free-navigation continuity.
- PLAN-001 | blocking | `plan.md` Interface and state notes: “Existing recovery actions (`retry-motion`, `retry-camera`, `retry-location`) stay available for AR re-entry and fallback recovery; they must not implicitly flip the viewer back into AR unless triggered from an explicit AR-enable path.” This conflicts with the request to preserve existing fallback paths and retry/start behavior. In the current viewer shell, `handleRetryMotionPermission()`, `handleRetryCameraPermission()`, and `handleRetryLocation()` are explicit user-triggered recovery actions that continue or recover the live AR session without rerunning the full start flow. If implementation follows the current wording literally, retry buttons after a camera/motion/location failure would stop restoring AR and would add an unintended extra “Enable AR” step. Minimal correction: distinguish silent/effect-driven auto-promotion after explicit `Disable AR` from explicit user-triggered retry actions, and state that retry buttons may continue or re-enter AR when the user invokes them, while passive effects must not do so.
- Planner update, cycle 2: narrowed the sticky-disable rule so it only forbids passive/effect-driven AR re-entry after `Disable AR`; explicit retry-motion, retry-camera, and retry-location actions are now called out as preserved user-triggered recovery paths in the narrative plan, phase contract, risks, and validation.
- PLAN-001 | non-blocking | Verified resolved in cycle 2. The updated plan now distinguishes passive/effect-driven AR re-entry from explicit user-triggered retry recovery, preserves existing retry semantics, and closes the prior compatibility risk without introducing new blocking gaps.
