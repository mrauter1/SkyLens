# Plan ↔ Plan Verifier Feedback
- 2026-03-26: Replaced the placeholder plan with a single implementation-ready phase that keeps the existing viewer pose contract stable while moving iOS Safari sensor normalization to a quaternion-first pipeline and locking the remediation behind targeted regression tests.
- PLAN-001 blocking: `plan.md` Validation / Rollback and `phase_plan.yaml` acceptance criteria rely on unit coverage only for an iPhone Safari real-world alignment bug. A browser-to-ENU quaternion or `webkitCompassHeading` convention mistake can still pass self-consistent unit tests while leaving live Safari pointing rotated or mirrored on device. Minimal correction: add explicit manual iPhone Safari validation and acceptance criteria for portrait and landscape heading alignment, >90-degree transitions, and recenter/offset behavior before implementation closeout.
- 2026-03-26: Added an explicit iPhone Safari smoke-validation gate to `plan.md` and `phase_plan.yaml`, including required on-device checks and fallback handling when that validation environment is unavailable, to close PLAN-001's real-world alignment gap.
- PLAN-002 non-blocking: No blocking verification findings remain. The plan now covers the requested quaternion-first remediation, >90-degree inversion fix, real-world alignment safeguards, regression tests, compatibility constraints, and explicit iPhone Safari validation/rollout gating.
