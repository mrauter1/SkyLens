<autoloop-decisions-header version="1" block_seq="1" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260326T213144Z-91b0386a" ts="2026-03-26T21:31:44.515971+00:00" />
The iOS Safari remediation must preserve the existing CameraPose, permission-state, recenter, and persisted viewer-settings contracts; quaternion-first is an internal sensor-pipeline change, not a viewer-state redesign.
Screen-orientation correction and Safari heading handling should be composed in quaternion math rather than by rotating raw alpha/beta/gamma fields, because the current Euler-first remap is the likely source of the >90-degree inversion and alignment drift.
Regression coverage for this task must assert world/projection alignment invariants in addition to angle continuity so mirrored-axis regressions are caught.
<autoloop-decisions-header version="1" block_seq="2" owner="planner" phase_id="task-global" pair="plan" turn_seq="2" run_id="run-20260326T213144Z-91b0386a" ts="2026-03-26T21:34:52.778358+00:00" />
Real-device iPhone Safari validation is required before closing this remediation; unit tests alone are not sufficient proof of corrected real-world alignment.
The required Safari smoke check must cover portrait and landscape alignment, >90-degree pitch transitions, and recenter plus persisted-offset behavior; if that environment is unavailable, the remaining validation gap must be recorded explicitly.
<autoloop-decisions-header version="1" block_seq="3" owner="implementer" phase_id="remediate-ios-safari-orientation" pair="implement" turn_seq="1" run_id="run-20260326T213144Z-91b0386a" ts="2026-03-26T21:36:01.473511+00:00" />
Screen-orientation remediation is implemented as a quaternion rotation on the existing camera basis using the camera-local down axis, then heading/pitch/roll are re-derived from the corrected quaternion so the external pose contract stays unchanged.
Angle smoothing stays in the continuous heading/pitch/roll domain even after quaternion-first normalization, because quaternion slerp reintroduced short-arc flips across zenith/nadir in the emitted subscription stream.
Manual iPhone Safari smoke validation is still unresolved in this environment; unit tests and lint are green, but the required portrait/landscape and >90-degree device check still needs a real Safari pass before closeout.
<autoloop-decisions-header version="1" block_seq="4" owner="implementer" phase_id="remediate-ios-safari-orientation" pair="implement" turn_seq="1" run_id="run-20260326T220259Z-d167eedb" ts="2026-03-26T22:03:00.035831+00:00" />
Landscape (`±90°`) pose-contract sign selection now runs after quaternion continuity resolution and only for samples still within the `[-90°, +90°]` pitch branch, so identical landscape quaternions normalize consistently without re-flipping >90° continuity samples.
Manual iPhone Safari smoke validation remains unavailable in this environment; the code fix is covered by targeted orientation regressions plus a full `npm test` pass, but the required real-device validation gap is still open.
<autoloop-decisions-header version="1" block_seq="5" owner="test_author" phase_id="remediate-ios-safari-orientation" pair="test" turn_seq="1" run_id="run-20260326T220259Z-d167eedb" ts="2026-03-26T22:11:25.005512+00:00" />
Unit coverage now locks both positive and negative `±90°` landscape branch selection directly by feeding the same zenith/nadir quaternions with and without prior portrait-history samples, while the subscription suite remains the guard for smoothed repeated-landscape branch continuity.
