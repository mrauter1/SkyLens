# Test Author ↔ Test Auditor Feedback

- Task ID: execute-the-movement-fidelity-plan-defined-in-mo-8533d92f
- Pair: test
- Phase ID: aircraft-temporal-continuity
- Phase Directory Key: aircraft-temporal-continuity
- Phase Title: Smooth aircraft motion between snapshots
- Scope: phase-local authoritative verifier artifact

- Added deterministic aircraft-layer coverage for the intermediate stale downgrade window (`motionState: 'stale'` with reduced `motionOpacity`) before full suppression, closing the remaining AC-2 coverage gap.
- Recorded a behavior-to-test map in `test_strategy.md` covering interpolation, bounded extrapolation, refresh failure retention, fade-out, stale downgrade, and final suppression.
- Added a viewer-level DOM assertion in `tests/unit/viewer-shell-celestial.test.ts` proving stale aircraft motion metadata reaches marker `aria-label` text and inline opacity, so the additive UI downgrade path cannot regress silently.
- `TST-001` `blocking` [tests/unit/aircraft-layer.test.ts, tests/unit/viewer-shell.test.ts, components/viewer/viewer-shell.tsx] The new tests cover motion-state math and retained-snapshot wiring, but they never exercise the viewer-shell rendering paths that consume `motionState` / `motionOpacity` (`formatSkyObjectSublabel`, `renderObjectBadges`, `getObjectMotionOpacity`). Concrete missed-regression scenario: the UI could stop surfacing stale/estimated aircraft labels, badges, or opacity changes, and the current suite would still pass because it only asserts metadata in `normalizeAircraftObjects`. Minimal correction: add one viewer-level DOM test with mocked aircraft objects carrying `motionState` and `motionOpacity`, and assert the stale/estimated treatment is actually rendered.
- `TST-001` resolved in part: `tests/unit/viewer-shell-celestial.test.ts` now asserts stale-aircraft marker `aria-label` text and inline opacity at the viewer DOM level, so the original metadata-only blind spot is no longer the whole story.
- `TST-002` `blocking` [tests/unit/viewer-shell-celestial.test.ts] The new stale-aircraft DOM assertion was added to a suite that still fails end-to-end: `npx vitest run tests/unit/viewer-shell-celestial.test.ts` returns non-zero because `renders a trail polyline for a trail-eligible object as scene time advances` still expects a `1000ms` interval and throws `No interval registered for 1000ms`. Concrete missed-regression scenario: the intended viewer-level stale-state gate stays buried inside a red suite, so routine file-level runs cannot cleanly distinguish a stale-aircraft regression from the unrelated trail harness failure. Minimal correction: move the aircraft stale UI assertion into a passing viewer suite or repair the broken interval-harness assumption in this file so the containing suite passes.
- `TST-003` `blocking` [components/viewer/viewer-shell.tsx, tests/unit/viewer-shell-celestial.test.ts] The current viewer-level assertions still cover only the stale marker `aria-label` and opacity path. They do not exercise the other user-visible aircraft motion disclosures introduced by this phase: `Estimated` sublabels and `renderObjectBadges(...)` for `estimated` / `stale` detail views. Concrete missed-regression scenario: bounded dead-reckoned aircraft could lose their `Estimated` disclosure or aircraft motion badges in the selected/center-locked panels while `tests/unit/aircraft-layer.test.ts`, `tests/unit/viewer-shell.test.ts`, and the new stale marker assertion all continue to pass. Minimal correction: add a viewer-level DOM test that injects an `estimated` aircraft, drives it into the selected or center-locked detail surface, and asserts the expected motion label/badge rendering there.
- Added viewer-level aircraft motion disclosure coverage to `tests/unit/viewer-shell.test.ts`, moving the stale marker assertions into a passing suite and adding an estimated-aircraft selected-detail assertion that exercises both the `Aircraft Estimated` sublabel and the `Estimated` motion badge.
- `TST-001` resolved: `tests/unit/viewer-shell.test.ts` now exercises the viewer-shell render paths that consume `motionState` / `motionOpacity`, including marker label/opacity and selected-detail disclosure.
- `TST-002` resolved: the phase-local stale-aircraft DOM coverage now lives in the passing `tests/unit/viewer-shell.test.ts` suite instead of the unrelated red celestial suite.
- `TST-003` resolved: estimated-aircraft UI disclosure is now asserted through both the marker sublabel and the selected-detail badge path.
- Re-audit result: no remaining blocking or non-blocking findings for the aircraft temporal continuity test scope. Phase-relevant validation passed with `npx vitest run tests/unit/aircraft-layer.test.ts tests/unit/viewer-shell.test.ts` (53 tests).
