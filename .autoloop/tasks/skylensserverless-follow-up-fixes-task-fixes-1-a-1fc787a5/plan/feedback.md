# Plan ↔ Plan Verifier Feedback

- 2026-03-28: Replaced the placeholder plan with a two-phase implementation plan covering the static export contract, affected script/test/doc updates, ordered TLE relay fallback behavior, and the required unit/build validation gates.
- PLAN-001 [blocking]: `plan.md` milestone 1, compatibility notes, and `decisions.txt` convert the current fork-owned `Permissions-Policy` header into a deployment-host responsibility without any explicit user confirmation for that compatibility break. The request says to keep current behavior parity intact, and the current fork contract still emits that header from `SkyLensServerless/next.config.ts` while `SkyLensServerless/tests/e2e/embed.spec.ts` asserts it. Minimal correction: either ask for explicit confirmation before accepting the header-ownership change, or revise the plan to preserve an equivalent supported header contract and its validation inside the requested workflow.
- PLAN-002 [non-blocking]: Fix 2 explicitly asks for structured relay diagnostics with group ID plus relay index/path, but the plan and phase acceptance criteria only require generic "relay attempt metadata" / "structured failure context per attempt." Tighten that wording so later turns do not satisfy the plan while omitting one of the requested diagnostic fields.
- 2026-03-28: Revised the plan after clarifications to preserve `Permissions-Policy` parity through a repo-owned static-host artifact/config inside `SkyLensServerless/`, updated the build/validation/docs contract around that artifact, and made the relay diagnostic fields explicit (`groupId`, relay index, relay path/template).
- PLAN-003 [non-blocking]: Re-review found no remaining blocking issues. PLAN-001 is addressed by the clarified repo-owned static-host artifact/config contract and the explicit parity-precedence rule, and PLAN-002 is addressed by the plan/phase requirement that relay diagnostics include `groupId`, relay index, and relay path/template.
