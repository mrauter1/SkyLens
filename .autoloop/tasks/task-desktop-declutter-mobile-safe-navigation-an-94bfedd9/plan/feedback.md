# Plan ↔ Plan Verifier Feedback

- 2026-03-28 planner: Replaced the empty plan with a two-phase implementation plan centered on `components/viewer/viewer-shell.tsx`, explicitly defining desktop shell simplification, desktop pointer-reticle hover semantics, compact top warnings, mobile navigation invariants, and the required unit/e2e regression coverage.
- PLAN-001 blocking: The plan does not lock down the required desktop control contract from the request. In `plan.md` Milestone 1, desktop controls are described as `Open Viewer`, a dynamic permission recovery button, and `Align`, but the user explicitly called for prominent streamlined controls covering `Open Viewer`, `Enable Camera`, `Motion`, and `Align`. As written, an implementation could ship a single recovery action and never expose distinct camera and motion affordances on desktop while still claiming conformance. Minimal correction: define the exact desktop action set by viewer state, or explicitly document how the requested camera and motion controls map onto dynamic buttons and mirror that in `phase_plan.yaml` acceptance criteria.
- PLAN-002 blocking: `phase_plan.yaml` phase `simplify-viewer-shell` can currently be satisfied by removing desktop panels without preserving access to the information they expose. AC-1 only requires that desktop “no longer renders the current stacked persistent summary/dock/privacy layout,” but it does not require object info, selected-object details, or other necessary recovery/detail surfaces to remain reachable through the new overlay/compact layout. That creates a concrete regression path where decluttering deletes functionality instead of reorganizing it. Minimal correction: update the narrative plan and phase acceptance criteria so decluttering must keep the relevant detail/recovery content accessible via the simplified overlay/top-rail model rather than merely removing the current panels.
- PLAN-003 non-blocking: The validation section would be safer if it named the concrete desktop interaction condition for the new hover tests, such as fine-pointer or desktop breakpoint coverage, to prevent “desktop hover” from being verified only through generic DOM assertions.
- 2026-03-28 planner update: Tightened the plan to explicitly require the desktop Open Viewer / Enable Camera / Motion / Align action row, preserved access to existing detail/recovery/privacy content after desktop decluttering, and added explicit desktop fine-pointer validation language to resolve the verifier findings.
- 2026-03-28 verifier: No remaining findings. PLAN-001, PLAN-002, and PLAN-003 are now addressed by the updated desktop action contract, preserved-content requirements, and explicit desktop fine-pointer validation language.
