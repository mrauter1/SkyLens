# Plan ↔ Plan Verifier Feedback
- 2026-04-05: Replaced the stub plan with a six-phase implementation plan aligned to the checked-in scope PRD/ARD sequence, added concrete module ownership and regression controls, and explicitly captured the dataset pipeline plus the user-provided R2 names input so later implement/test turns do not under-scope the work.
- PLAN-001 | blocking | Missing explicit scope-toggle availability gating. Affected sections: `plan.md` milestone 2 and `phase_plan.yaml` phase `scope-settings-controls`. The PRD requires scope mode and all toggle entry points to appear only when the viewer is active and not in blocked startup states; the current plan only says to add synchronized toggles and to guard rendering behind viewer-active/non-blocked states. Failure scenario: later turns can correctly hide the lens but still show dead scope controls in blocked startup or unsupported secure-context states, violating FR-1/FR-2 and creating unintended UI behavior. Minimal correction: add explicit control-visibility/availability rules and matching acceptance criteria/tests for blocked startup, unsupported secure-context, live active, demo, and fallback states.
- PLAN-002 | blocking | The provided R2 `names.json` input is acknowledged but not mapped to an exact deterministic build/runtime contract. Affected sections: `plan.md` milestone 4, `phase_plan.yaml` phase `scope-dataset-pipeline`, and the matching decision entry. The checked-in dataset PRD/ADR fixes name augmentation precedence and determinism requirements, but the plan does not state whether the R2 file is a production-only source artifact, a build input with defined precedence, or a direct runtime replacement. Failure scenario: later turns may inject remote names at the wrong layer, break offline dev fallback, or violate the fixed manual-override/bright-star/unnamed precedence and emitted-`names.json` determinism rules. Minimal correction: state the exact role of the R2 dataset, its precedence relative to manual overrides and the bright-star join, when it is allowed to affect emitted `public/data/scope/v1/names.json`, and how `scope:data:verify` and `scope:data:build:dev` remain deterministic and offline.
- 2026-04-05: Updated the plan to resolve `PLAN-001` and `PLAN-002` by adding explicit scope-control availability gating across all entry points and by defining the provided R2 `names.json` as a production-only optional names-table input that cannot change the fixed local augmentation precedence or offline deterministic dev/verify flows.
- PLAN-003 | non-blocking | Recheck complete: `PLAN-001` and `PLAN-002` are resolved in the current `plan.md`, `phase_plan.yaml`, and turn-2 `decisions.txt` block. No new plan-level findings were identified.
