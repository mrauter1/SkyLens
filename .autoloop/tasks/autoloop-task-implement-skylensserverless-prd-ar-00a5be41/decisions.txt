<autoloop-decisions-header version="1" block_seq="1" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260405T113744Z-0aa71afd" ts="2026-04-05T11:37:44.757297+00:00" />
Preserve `/view` as the only viewer route; scope extends the existing `ViewerShell` and viewer-settings flow instead of adding a separate route or storage namespace.
Keep deep scope stars out of the existing clickable marker path; only bright scope objects and named deep scope stars participate in scope center-lock and detail precedence.
Treat the provided R2 `names.json` as implementation input for scope name data while keeping the shipped runtime contract as repo-local `public/data/scope/v1/names.json` and preserving offline development fallback behavior.
Commit only the deterministic development runtime dataset under `public/data/scope/v1`; keep raw or downloaded production source artifacts under ignored `.cache/scope-source/`.
<autoloop-decisions-header version="1" block_seq="2" owner="planner" phase_id="task-global" pair="plan" turn_seq="2" run_id="run-20260405T113744Z-0aa71afd" ts="2026-04-05T11:42:26.239182+00:00" />
Scope controls must be hidden, not merely disabled, whenever the viewer is inactive, blocked in startup, or unsupported; availability gating applies to settings, mobile quick actions, and desktop actions.
The provided R2 `names.json` is a production-only optional names-table input and not a new row-matching precedence layer; local build precedence remains manual override, then built-in bright-star join, then unnamed.
`scope:data:build:dev` and offline `scope:data:verify` must remain deterministic and repo-local without requiring the R2 names source; emitted `public/data/scope/v1/names.json` still contains emitted rows only.
<autoloop-decisions-header version="1" block_seq="3" owner="implementer" phase_id="projection-profile-foundation" pair="implement" turn_seq="1" run_id="run-20260405T114452Z-fe1960c6" ts="2026-04-05T11:44:52.766908+00:00" />
Projection profiles now use a generic 1-179 degree clamp while the existing wide-view wrappers still route through the historical 20-100 degree calibrated range, so future scope consumers can request narrower FOVs without changing current wide behavior.
<autoloop-decisions-header version="1" block_seq="4" owner="test_author" phase_id="projection-profile-foundation" pair="test" turn_seq="1" run_id="run-20260405T114452Z-fe1960c6" ts="2026-04-05T11:49:32.589177+00:00" />
Projection profile coverage stays in `tests/unit/projection-camera.test.ts` and asserts pure helper behavior directly, because this phase changes `lib/projection/camera.ts` math and compatibility wrappers without introducing broader viewer integration paths.
<autoloop-decisions-header version="1" block_seq="5" owner="implementer" phase_id="scope-settings-controls" pair="implement" turn_seq="1" run_id="run-20260405T114452Z-fe1960c6" ts="2026-04-05T11:51:20.030437+00:00" />
Persist scope settings under the existing viewer-settings payload as `scope.enabled` and `scope.verticalFovDeg`, with centralized defaults/clamping in `lib/viewer/settings.ts` so older saved payloads remain readable.
Gate scope controls from one `ViewerShell` availability check (`experience.mode !== 'blocked'`) and reuse the same `viewerSettings.scope` state for settings-sheet, desktop, and mobile actions to keep all entry points synchronized.
<autoloop-decisions-header version="1" block_seq="6" owner="implementer" phase_id="lens-shell-bright-objects" pair="implement" turn_seq="1" run_id="run-20260405T114452Z-fe1960c6" ts="2026-04-05T12:00:41.268230+00:00" />
Scope bright-object lens rendering reuses the existing normalized sun/moon/planet/star scene objects and runs a second projection pass into a square lens viewport, so scope center-lock can swap away from wide-only candidates without mutating the wide marker path.
The lens background uses a separate clipped video element bound to the active stream, with the existing gradient/grid fallback reused when camera imagery is unavailable, so the lens can visually occlude and pointer-shield the center area without disturbing the base stage.
Wide-scene highlighting, daylight exceptions, label ranking, and focused aircraft trails continue to use the original wide center-lock candidate even when scope mode swaps the centered readout to a scope bright object, preserving outside-lens behavior.
The scope lens hit region now uses a clip-path circle on the interactive layer with chrome rendered on non-interactive sibling layers, so only the visible lens interior intercepts pointer events.
<autoloop-decisions-header version="1" block_seq="7" owner="test_author" phase_id="lens-shell-bright-objects" pair="test" turn_seq="1" run_id="run-20260405T114452Z-fe1960c6" ts="2026-04-05T12:19:57.506536+00:00" />
Lens-shell bright-object coverage now includes an explicit DOM-order regression asserting `scope-lens-overlay` renders after wide constellation lines, markers, and labels, closing the prior occlusion-stack gap without broadening phase scope.
<autoloop-decisions-header version="1" block_seq="8" owner="implementer" phase_id="scope-dataset-pipeline" pair="implement" turn_seq="1" run_id="run-20260405T114452Z-fe1960c6" ts="2026-04-05T12:23:14.737274+00:00" />
The scope dataset pipeline now treats the provided R2 `names.json` as an optional production-only cached input under `.cache/scope-source/published/names.json`, validates its schema when present, and deliberately keeps it out of emitted row naming so manual overrides, bright-star HIP joins, emitted-row-only `names.json`, and offline dev/verify determinism do not drift.
The committed dev dataset is built from six deterministic synthetic rows per `stars_200.json` seed entry, with only synthetic index `5` carrying the normalized source name; this preserves cumulative-band depth and guarantees a named deep-star path without changing the wide bright-star layer itself.
<autoloop-decisions-header version="1" block_seq="9" owner="implementer" phase_id="scope-dataset-pipeline" pair="implement" turn_seq="2" run_id="run-20260405T114452Z-fe1960c6" ts="2026-04-05T12:39:51.683212+00:00" />
Manual override resolution now collects all matching manual candidates per normalized source row and hard-fails on mixed `HIP` + `TYC` matches before applying the bright-star HIP join, matching the ADR requirement that multiple override rows targeting one source row are invalid.
<autoloop-decisions-header version="1" block_seq="10" owner="implementer" phase_id="projection-profile-foundation" pair="implement" turn_seq="1" run_id="run-20260405T135546Z-910a320a" ts="2026-04-05T13:55:46.555602+00:00" />
This resume turn made no additional `SkyLensServerless` source edits for `projection-profile-foundation`; the previously landed profile-aware projection API and regression tests already satisfied the scoped acceptance criteria, so the turn focused on revalidation after restoring local dependencies with `npm ci`.
<autoloop-decisions-header version="1" block_seq="11" owner="test_author" phase_id="projection-profile-foundation" pair="test" turn_seq="1" run_id="run-20260405T135546Z-910a320a" ts="2026-04-05T13:59:04.378301+00:00" />
Resume-turn projection coverage adds a direct raw-profile clamp regression in `tests/unit/projection-camera.test.ts` so future scope callers that construct `ProjectionProfile` objects manually still inherit the helper-level `1..179` safety bounds.
<autoloop-decisions-header version="1" block_seq="12" owner="implementer" phase_id="scope-settings-controls" pair="implement" turn_seq="1" run_id="run-20260405T135546Z-910a320a" ts="2026-04-05T14:01:00.162342+00:00" />
The startup-route regression coverage in `tests/unit/viewer-shell.test.ts` now carries an explicit `10_000` ms timeout because the full `ViewerShell` suite can exceed Vitest's default `5_000` ms budget even when the orientation-ready behavior still passes; keeping the timeout local avoids broadening global test budgets.
<autoloop-decisions-header version="1" block_seq="13" owner="test_author" phase_id="scope-settings-controls" pair="test" turn_seq="1" run_id="run-20260405T135546Z-910a320a" ts="2026-04-05T14:05:31.109497+00:00" />
Scope-settings test strategy now treats the `ViewerShell` orientation-ready route regression as a localized timing risk and documents the single-test `10_000` ms timeout as the stabilization boundary, so future edits do not respond by weakening broader suite timing expectations.
<autoloop-decisions-header version="1" block_seq="14" owner="implementer" phase_id="lens-shell-bright-objects" pair="implement" turn_seq="1" run_id="run-20260405T135546Z-910a320a" ts="2026-04-05T14:07:25.490176+00:00" />
Resume revalidation found the `IMP-001` and `IMP-002` fixes already present in `ViewerShell` and `ScopeLensOverlay`, so this turn made no additional `SkyLensServerless` source edits and treated targeted passing tests as the phase-completion signal.
<autoloop-decisions-header version="1" block_seq="15" owner="implementer" phase_id="scope-dataset-pipeline" pair="implement" turn_seq="1" run_id="run-20260405T135546Z-910a320a" ts="2026-04-05T14:13:51.229527+00:00" />
Resume revalidation found the scope-dataset-pipeline reviewer fix already present in `scripts/scope/build-core.mjs` and `tests/unit/scope-data-names.test.ts`; this turn made no additional `SkyLensServerless` source edits after the targeted scope-data suite, offline verify, and deterministic dev rebuild all passed without regenerating drift.
<autoloop-decisions-header version="1" block_seq="16" owner="test_author" phase_id="scope-dataset-pipeline" pair="test" turn_seq="1" run_id="run-20260405T135546Z-910a320a" ts="2026-04-05T14:15:58.799753+00:00" />
Scope-data name-collision coverage now treats any mixed `HIP` + `TYC` manual override double-match as invalid even when both override rows normalize to the same display name, preventing future tests from silently redefining the conflict rule as value-based deduplication.
<autoloop-decisions-header version="1" block_seq="17" owner="implementer" phase_id="dense-scope-canvas-runtime" pair="implement" turn_seq="1" run_id="run-20260405T135546Z-910a320a" ts="2026-04-05T14:17:43.612757+00:00" />
Dense scope runtime now keeps manifest, names, band indexes, and decoded tile rows in module-level session caches under `lib/scope/catalog.ts`, while `ViewerShell` uses generation trackers to ignore stale catalog/tile responses after scope disable, band changes, or tile-set changes without abort requirements.
`ViewerShell` keeps wide center-lock/highlighting behavior untouched outside the lens and resolves scope center-lock with explicit precedence `scope bright object -> named deep scope star -> none` when scope mode is enabled, so wide center-lock no longer back-fills the scope chip after a deep-scope miss.
Named deep scope stars are projected from cached tile rows via `lib/scope/coordinates.ts`, remain canvas-only through `ScopeStarCanvas`, and surface into centered readout/detail only by constructing scope-only summary objects instead of entering the wide marker/button inventory.
<autoloop-decisions-header version="1" block_seq="18" owner="implementer" phase_id="dense-scope-canvas-runtime" pair="implement" turn_seq="2" run_id="run-20260405T135546Z-910a320a" ts="2026-04-05T14:32:08.245169+00:00" />
Scope tile selection now expands RA bounds from the worst absolute declination reached by the padded view (`max(abs(queryDecMin), abs(queryDecMax))`) rather than from `centerDecDeg`, preventing high-declination lens views from dropping tiles that are visible near the top or bottom edge.
<autoloop-decisions-header version="1" block_seq="19" owner="test_author" phase_id="dense-scope-canvas-runtime" pair="test" turn_seq="1" run_id="run-20260405T135546Z-910a320a" ts="2026-04-05T14:34:23.035147+00:00" />
Dense-scope runtime coverage now includes both the worst-declination RA-widening regression and the explicit pole-reaching branch in `tests/unit/scope-runtime.test.ts`, so future tile-selection edits must preserve wrapped high-declination coverage as well as the “select every RA tile in-band at the pole” fallback.
<autoloop-decisions-header version="1" block_seq="20" owner="implementer" phase_id="validation-and-hardening" pair="implement" turn_seq="1" run_id="run-20260405T135546Z-910a320a" ts="2026-04-05T14:36:19.411629+00:00" />
Persisted scope-on state now delays `ScopeLensOverlay` rendering until the client hydration-ready gate (`hasMounted`) is true, preventing SSR/client markup divergence on reload while preserving scope persistence once the viewer becomes interactive.
<autoloop-decisions-header version="1" block_seq="21" owner="implementer" phase_id="validation-and-hardening" pair="implement" turn_seq="1" run_id="run-20260405T150932Z-a34e199f" ts="2026-04-05T15:09:33.982146+00:00" />
<autoloop-decisions-header version="1" block_seq="22" owner="implementer" phase_id="validation-and-hardening" pair="implement" turn_seq="1" run_id="run-20260405T185342Z-408c3b0f" ts="2026-04-05T18:53:42.432865+00:00" />
Validation-and-hardening closed without new `SkyLensServerless` source edits after the full lint/unit/e2e/dataset/build matrix passed; Playwright required browser plus Linux runtime dependency installation in the execution environment, and repo scanning confirmed no WebGL/WebGPU/hardware zoom paths were added.
