# Plan ↔ Plan Verifier Feedback

- Planned one coherent implementation slice: add browser-safe remote scope config, make the scope loader remote-first with local fallback and source-aware caches, slim the committed dev dataset/export mirror, and cover the three required runtime test paths. This keeps the manifest/tile schema unchanged and preserves the existing graceful-degradation contract.
- PLAN-001 `blocking`: The plan does not define a concrete slimming contract for the committed dev dataset, so the main user goal of keeping only a minimal in-git fallback set is not yet implementation-ready. Evidence: `plan.md` section `Minimal committed dev dataset` and `phase_plan.yaml` AC-3 say the dataset should be `minimal` / `intentionally small`, but they do not specify a deterministic mechanism or measurable bound. The current broader dev dataset already satisfies the listed invariants (`mag6p5` present, deeper band richer, named row exists), so an implementation could leave the dataset materially large and still claim compliance. Minimal correction: specify the exact slimming strategy to implement and test, such as a fixed curated seed subset plus exact or upper-bound row/tile assertions, or another explicit deterministic size contract.
- PLAN-002 `non-blocking`: Export-artifact handling is still conditional where the repo currently tracks `SkyLensServerless/out/data/scope/v1`. Evidence: `plan.md` says to regenerate `out/data/scope/v1` `if repository policy continues to track export artifacts`, while the tracked baseline already includes those files. Minimal correction: make the plan explicit that the tracked export mirror must be refreshed in the same change, or explicitly call out a confirmed repo-policy change if those artifacts are to stop being maintained.
- Producer update, cycle 2: resolved `PLAN-001` by defining a fixed 12-seed dev fallback strategy with explicit `12/24/48/72` per-band manifest totals, demo-sky coverage, and a required tile-file cap assertion; resolved `PLAN-002` by making `SkyLensServerless/out/data/scope/v1` refresh mandatory in the same slice as `public/data/scope/v1`.
- Verifier update, cycle 2: no remaining blocking findings. `PLAN-001` is considered resolved because the plan now defines a deterministic 12-seed slimming contract with measurable manifest totals and footprint controls. `PLAN-002` is considered resolved because the plan now makes the tracked `out/data/scope/v1` refresh mandatory rather than conditional.
