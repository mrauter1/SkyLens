# Plan ↔ Plan Verifier Feedback
- 2026-04-07: Replaced empty plan artifacts with an implementation-ready runtime split. The plan now fixes three current mismatches with the request: scope-only optics ownership, magnification-coupled emergence math, and label-rank-based center-lock ordering.
- PLAN-001 | blocking | `plan.md` Milestone 2 and `phase_plan.yaml` phase `unify-optics-runtime` do not explicitly plan the concrete runtime change that makes HYG deep-star loading/tile selection active outside scope mode. In the current code, deep-star fetch/render is gated by `scopeModeActive` and uses the square lens viewport/FOV for selection. Without an explicit step for mode-independent deep-star activation plus the main-view selection/projection inputs, the requested main-view always-active optics and deep-star participation in center-lock/labels cannot be implemented safely. Minimal correction: add a concrete plan item and acceptance coverage for decoupling deep-star request gating from scope presentation while preserving invalidation/cancellation.
- PLAN-002 | non-blocking | `phase_plan.yaml` deliverables for `unify-optics-runtime` describe planning outputs rather than the actual code/test artifacts the implementation phase should land. Minimal correction: rewrite those deliverables in terms of concrete implementation outputs so handoff is crisper.
- 2026-04-07: Revised the plan to close `PLAN-001` by adding an explicit mode-independent `DeepStarRequestContext`, main-view viewport/FOV tile-selection inputs, and acceptance/test coverage for deep-star loading outside scope mode. Also rewrote phase-one deliverables as concrete implementation outputs to address `PLAN-002`.
- PLAN-001 | resolved | The revised plan now explicitly covers mode-independent HYG deep-star request gating, main-view viewport/FOV tile-selection inputs, and matching acceptance/test coverage.
- PLAN-002 | resolved | The phase-one deliverables now describe concrete implementation outputs instead of planning outputs.
