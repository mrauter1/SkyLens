# Plan ↔ Plan Verifier Feedback

- Replaced placeholder plan artifacts with a three-phase implementation plan covering browser-direct fetch/contracts, tracker/viewer integration, and cleanup/validation so later turns have concrete interfaces, regression controls, and ordered acceptance criteria.
- PLAN-001 | blocking | `plan.md` sections `lib/aircraft/opensky-browser.ts`, `components/viewer/viewer-shell.tsx`, and `Risk Register` conflict with the request’s failure semantics. The plan says the browser fetcher should "return degraded empty response rather than throwing", but the request explicitly requires `ViewerShell` failure handling to set availability degraded and "do not clear tracker", and the requested tests include failed polls retaining aircraft. If the fetcher resolves to an empty degraded snapshot, the success path will ingest that empty snapshot and can prune or clear visible aircraft, causing the exact regression the request forbids. Minimal correction: make the plan distinguish transport failure from successful degraded payloads, and require the polling loop to avoid ingesting/clearing tracker state when the upstream fetch fails or rate-limits.
- PLAN-002 | non-blocking | `plan.md` does not pin concrete ownership for the requested config rewrites in `lib/config.ts`, especially `POLL_INTERVAL_MS_BY_QUALITY`, the 180 km default radius, and the privacy copy update. The current narrative mentions "tracker/config usage" and generic config cleanup, which is directionally correct but still leaves room for scattered constants between `viewer-shell`, tracker code, and config. Tightening the plan to state which constants live in `lib/config.ts` versus `lib/aircraft/tracker.ts` would reduce drift risk during implementation.
- Revised the plan to resolve PLAN-001 by changing browser fetch failure semantics from degraded-empty-response ingestion to typed transport/rate-limit failures that `ViewerShell` handles by degrading availability without mutating tracker state, and to resolve PLAN-002 by assigning public aircraft cadence/default/privacy ownership to `lib/config.ts`.
- PLAN-001 | non-blocking | Verified resolved in the current revision. The plan now requires typed browser fetch failures and explicitly states that failed polls must degrade availability without ingesting empty placeholder data or clearing tracker state.
- PLAN-002 | non-blocking | Verified resolved in the current revision. The plan now assigns public aircraft cadence/default/privacy ownership to `lib/config.ts` and keeps tracker/prediction/trail constants in `lib/aircraft/tracker.ts`.
