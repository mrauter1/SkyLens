<autoloop-decisions-header version="1" block_seq="1" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260328T131554Z-891540b6" ts="2026-03-28T13:15:54.562513+00:00" />
Aircraft fetching must degrade in-place and retain tracker state on failures; missed polls are not allowed to clear aircraft immediately.
The tracker contract is the new single aircraft motion source for both live and demo rendering; legacy previousSnapshot/transitionStartedAtMs state is intentionally removed rather than adapted.
/api/aircraft, aircraftCache health output, and OpenSky auth environment-variable support are intentional compatibility breaks required by the request and must be cleaned from code, tests, and docs together.
<autoloop-decisions-header version="1" block_seq="2" owner="planner" phase_id="task-global" pair="plan" turn_seq="2" run_id="run-20260328T131554Z-891540b6" ts="2026-03-28T13:18:52.919456+00:00" />
Transport failures, non-OK OpenSky responses, and 429 rate limits must stay distinct from successful aircraft snapshots so ViewerShell can mark availability degraded without ingesting empty placeholder data into the tracker.
lib/config.ts owns public aircraft defaults and polling/privacy configuration; lib/aircraft/tracker.ts owns only tracker/prediction/trail constants.
<autoloop-decisions-header version="1" block_seq="3" owner="implementer" phase_id="aircraft-browser-contracts-and-fetch" pair="implement" turn_seq="1" run_id="run-20260328T131554Z-891540b6" ts="2026-03-28T13:20:38.165550+00:00" />
Split-browser OpenSky responses now merge by `icao24`, preferring the sample with the newest `timePositionS` and then `lastContactS`; the exposed `snapshotTimeS` is the maximum top-level `time` across the merged responses.
`lib/aircraft/opensky.ts` is temporarily retained only as a compatibility shim over the anonymous browser fetcher so current route/health imports keep compiling during this phase; no aircraft auth or cache logic remains in that module.
The temporary `lib/aircraft/opensky.ts` shim preserves current `/api/health` compatibility by tracking only latest successful fetch timestamps/expiry metadata, not cached aircraft payloads or auth state.
<autoloop-decisions-header version="1" block_seq="4" owner="test_author" phase_id="aircraft-browser-contracts-and-fetch" pair="test" turn_seq="1" run_id="run-20260328T131554Z-891540b6" ts="2026-03-28T13:33:11.098487+00:00" />
Phase-local tests intentionally keep `/api/health` compatibility coverage active until the later health-cleanup phase removes `aircraftCache`; the test asserts metadata freshness only and does not normalize any return to payload caching.
<autoloop-decisions-header version="1" block_seq="5" owner="implementer" phase_id="tracker-viewer-integration" pair="implement" turn_seq="1" run_id="run-20260328T131554Z-891540b6" ts="2026-03-28T13:35:18.127299+00:00" />
ViewerShell now owns a single `AircraftTracker` ref for both live and demo rendering; live polls only ingest successful snapshots, while demo mode feeds deterministic synthetic snapshots on 10-second buckets so demo aircraft keep moving without network access or stale-drop pop-out.
Focused aircraft trails are rendered from `tracker.getTrail()` screen-projected each frame, while the existing motion-affordance overlay remains separate and unchanged for generic moving-object UX.
The live aircraft poll loop uses one abortable timeout instead of a fixed interval so motion-quality cadence can switch between 30s/15s/10s and high-quality mode can align follow-up polls to the next OpenSky 10-second bucket using the latest successful `snapshotTimeS`.
High-quality aircraft polling must fall back to the normal 10-second cadence whenever the last successful `snapshotTimeS` is already behind the current bucket; stale timestamps are not allowed to trigger rapid retry loops after delayed or failed responses.
Tracker turn hysteresis is driven by the newest per-interval observed turn-rate sample for consecutive enter/exit counting, while the weighted multi-interval average turn rate remains reserved for prediction smoothing only.
<autoloop-decisions-header version="1" block_seq="6" owner="implementer" phase_id="cleanup-tests-and-validation" pair="implement" turn_seq="1" run_id="run-20260328T131554Z-891540b6" ts="2026-03-28T13:57:29.326531+00:00" />
`/api/health` now intentionally reports only `app` and `tleCache`; aircraft cache metadata was removed with the server aircraft path and must not be reintroduced as a compatibility shim.
`tests/unit/aircraft-layer.test.ts` was deleted instead of ported because `/api/aircraft` is an intentional removal and equivalent aircraft coverage now belongs in the browser client, tracker, and viewer-shell suites.
