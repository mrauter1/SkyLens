<autoloop-decisions-header version="1" block_seq="1" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260326T002617Z-38cf7947" ts="2026-03-26T00:26:17.578380+00:00" />
Treat the repository as a greenfield implementation, but lock the first introduced API shapes, env vars, settings keys, and bundled data paths to the PRD so later phases do not drift.
Keep live mode and demo mode on the same normalization, projection, ranking, and detail-card pipeline; only the data source, observer/time source, and background are allowed to differ.
Preserve the PRD milestone order as the execution order because sensor/projection contracts are prerequisites for every overlay layer and the label/ranking system depends on all prior object normalization work.
Treat deterministic math, visibility, and stable object identity as the main regression surface; fixture-backed tests for those behaviors are required before late UI polish.
<autoloop-decisions-header version="1" block_seq="2" owner="planner" phase_id="task-global" pair="plan" turn_seq="2" run_id="run-20260326T002617Z-38cf7947" ts="2026-03-26T00:29:58.199744+00:00" />
/api/config is a milestone-1 public bootstrap contract and must ship with the PRD-defined defaults, satellite groups, and build-version surfacing rather than being deferred behind later data-layer work.
The required trust copy is part of the implementation contract for the landing and viewer shell, not optional polish.
lib/cache ownership starts in the satellite/TLE phase and is reused by the aircraft phase to avoid duplicated cache seams.
<autoloop-decisions-header version="1" block_seq="3" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260326T003143Z-89d950dc" ts="2026-03-26T00:31:44.062759+00:00" />
Treat route-based code splitting between `/` and `/view`, the PRD-required script names, and the named fixture files as implementation-contract requirements rather than optional polish.
The aircraft degraded path must return empty aircraft results plus an explicit availability signal so the UI can surface `Live aircraft temporarily unavailable` without inferring outage state from transport errors.
Astronomy pipeline failure is a critical degraded mode that must hard-fail into demo mode with a visible banner instead of leaving the live viewer partially active.
<autoloop-decisions-header version="1" block_seq="4" owner="planner" phase_id="task-global" pair="plan" turn_seq="2" run_id="run-20260326T003143Z-89d950dc" ts="2026-03-26T00:34:59.525290+00:00" />
Treat the PRD detail-card field sets as locked implementation contracts per object type; producers should not replace them with generic metadata-only cards.
The settings-sheet `Enter demo mode` action is required product scope in the ranking/settings phase and must reuse the same shared demo pipeline as landing and fallback entry points.
Center-lock uses the PRD-fixed 4-degree angular radius and should stay explicit in the plan and tests.
<autoloop-decisions-header version="1" block_seq="5" owner="implementer" phase_id="app-shell-and-permissions" pair="implement" turn_seq="1" run_id="run-20260326T004506Z-29ea09c8" ts="2026-03-26T00:45:06.467160+00:00" />
Use one shared `ViewerRouteState` query-parameter contract for landing start, viewer retry, and demo entry so every M1 permission outcome maps into the same `/view` shell and later phases can extend one state seam instead of duplicating fallback logic.
Request camera permission during the Start flow with the PRD-required `getUserMedia` constraints, then stop the probe stream immediately; the actual live camera attachment is deferred to the sensor/camera milestone without changing the M1 permission-order contract.
Keep the settings sheet in M1 as a shell-level placeholder with live layer toggles and `Enter demo mode`, while leaving alignment and recenter actions visibly present but disabled until their sensor behavior exists in later milestones.
<autoloop-decisions-header version="1" block_seq="6" owner="implementer" phase_id="sensors-and-projection-foundation" pair="implement" turn_seq="1" run_id="run-20260326T004506Z-29ea09c8" ts="2026-03-26T01:12:40.070333+00:00" />
<autoloop-decisions-header version="1" block_seq="7" owner="implementer" phase_id="sensors-and-projection-foundation" pair="implement" turn_seq="2" run_id="run-20260326T004506Z-29ea09c8" ts="2026-03-26T01:34:37.324152+00:00" />
Keep `/view` entries with any `unknown` permission field in a preflight state; they must not present manual-pan mode or manual control badges until the route carries a complete verified permission payload.
Keep the viewer-shell sensor/camera state local to the component rather than returning refs and state through a custom hook, because the current React lint/compiler rules treat that pattern as unsafe render-time ref access even when the runtime behavior is correct.
Ignore generated Playwright output directories in the ESLint flat config so `npm run lint` stays deterministic before and after browser test runs.
<autoloop-decisions-header version="1" block_seq="8" owner="implementer" phase_id="sensors-and-projection-foundation" pair="implement" turn_seq="3" run_id="run-20260326T004506Z-29ea09c8" ts="2026-03-26T01:48:05.683822+00:00" />
Gate all live viewer side effects behind the shared `hasVerifiedLiveViewerState` predicate so blocked preflight `/view` states cannot arm geolocation, camera, or orientation work from URL fields alone.
Test coverage must keep `hasVerifiedLiveViewerState` true for non-camera and manual-pan fallback states, because those routes still need the normalized live viewer pipeline even when one sensor class is degraded.
<autoloop-decisions-header version="1" block_seq="9" owner="implementer" phase_id="app-shell-and-permissions" pair="implement" turn_seq="1" run_id="run-20260326T020604Z-abe9114e" ts="2026-03-26T02:06:05.069106+00:00" />
When a rerun of this phase finds the checked-in M1 shell already satisfying the active contract, prefer validation plus artifact refresh over churning product code without a failing acceptance signal.
<autoloop-decisions-header version="1" block_seq="10" owner="test_author" phase_id="app-shell-and-permissions" pair="test" turn_seq="1" run_id="run-20260326T020604Z-abe9114e" ts="2026-03-26T02:08:35.720147+00:00" />
Lock AC-1 coverage with direct unit tests for the manifest contract and viewer-shell trust copy so PWA metadata or `/view` privacy messaging regressions are caught without relying only on browser-level shell smoke tests.
<autoloop-decisions-header version="1" block_seq="11" owner="implementer" phase_id="sensors-and-projection-foundation" pair="implement" turn_seq="1" run_id="run-20260326T020604Z-abe9114e" ts="2026-03-26T02:10:23.003564+00:00" />
When a rerun of the sensors-and-projection-foundation phase finds the checked-in sensor math, viewer harness, and regression coverage already green, prefer validation plus artifact refresh over touching stable projection or pose code without a failing acceptance signal.
<autoloop-decisions-header version="1" block_seq="12" owner="implementer" phase_id="celestial-layer" pair="implement" turn_seq="1" run_id="run-20260326T020604Z-abe9114e" ts="2026-03-26T02:14:29.280130+00:00" />
Bundle `public/data/stars_200.json` from a fixed top-200 bright-star reduction and keep `public/data/constellations.json` constrained to segments whose star IDs exist in that bundled catalog, so the celestial layer stays deterministic without runtime catalog fetches.
Keep the full visible-star set available to constellation eligibility and card metadata, and apply label-count suppression only in the viewer shell, because trimming the star pipeline earlier starves valid constellation overlays.
Handle critical live astronomy failures in the viewer by deferring the transition into demo mode through the existing route-state shell and showing a hard fallback banner, so the app never keeps rendering a partial live celestial scene and still satisfies the current React effect constraints.
Keep the bottom dock owned by the active center-lock target or its fallback hint, and render tapped-label details in a separate selected-object card, so AC-3 stays true even after the user taps a non-centered celestial label.
<autoloop-decisions-header version="1" block_seq="13" owner="test_author" phase_id="celestial-layer" pair="test" turn_seq="1" run_id="run-20260326T020604Z-abe9114e" ts="2026-03-26T02:33:26.288766+00:00" />
Cover bottom-dock center-lock behavior in viewer-shell tests with mocked celestial-scene inputs, so the dock/hint/selected-card contract is isolated from sensor startup timing and deterministic astronomy fixtures can stay focused on math and normalization.
<autoloop-decisions-header version="1" block_seq="14" owner="implementer" phase_id="satellite-layer" pair="implement" turn_seq="1" run_id="run-20260326T020604Z-abe9114e" ts="2026-03-26T02:38:05.772280+00:00" />
Map the public `brightest` satellite group id to CelesTrak's `visual` TLE feed internally, while keeping the locked `/api/tle` and `/api/config` group id as `brightest`.
Treat the TLE stale window as 24 hours after `expiresAt` rather than 24 hours total from `fetchedAt`, yielding a 6-hour fresh window plus up to 24 more hours of stale serving.
Isolate satellite fetch or propagation failures from the critical astronomy fallback path; missing or bad TLE data must hide satellites only and must not force demo mode.
<autoloop-decisions-header version="1" block_seq="15" owner="test_author" phase_id="satellite-layer" pair="test" turn_seq="1" run_id="run-20260326T020604Z-abe9114e" ts="2026-03-26T02:52:37.548352+00:00" />
Lock the non-critical satellite-failure behavior in viewer-shell tests by making satellite normalization throw under a live route and asserting the app stays live instead of reusing the astronomy demo fallback path.
<autoloop-decisions-header version="1" block_seq="16" owner="implementer" phase_id="aircraft-layer" pair="implement" turn_seq="1" run_id="run-20260326T020604Z-abe9114e" ts="2026-03-26T02:54:09.427890+00:00" />
Cache OpenSky raw state vectors by rounded 0.1-degree bucket center plus radius, then recompute observer-relative azimuth/elevation/range per request so equivalent bucket hits reuse the 10-second fetch without reusing stale observer altitude math.
Treat aircraft upstream failure as a non-critical degraded state that always returns HTTP 200 with `availability: degraded` and an empty list; only invalid query input returns 400.
Surface degraded aircraft availability through the settings sheet label seam now so later satellite availability messaging can reuse the same UI contract without adding a second outlet.
<autoloop-decisions-header version="1" block_seq="17" owner="implementer" phase_id="aircraft-layer" pair="implement" turn_seq="2" run_id="run-20260326T020604Z-abe9114e" ts="2026-03-26T03:11:00.000000+00:00" />
Keep the aircraft cache key bucketed at 0.1 degrees, but expand the fetched OpenSky bounding box by the half-bucket footprint so valid edge-of-bucket observers do not silently lose in-radius aircraft while still reusing one cache entry.
Lock the authenticated aircraft path with tests for both first-pass token success and one-time 401 token refresh, because the auth branch is a required contract in this phase and anonymous-only coverage is insufficient.
<autoloop-decisions-header version="1" block_seq="18" owner="test_author" phase_id="aircraft-layer" pair="test" turn_seq="1" run_id="run-20260326T020604Z-abe9114e" ts="2026-03-26T03:14:03.462219+00:00" />
Lock the credential-present fallback contract with a unit test where authenticated `/states/all` fetch fails but anonymous latest-state access succeeds, so future changes cannot silently degrade the required auth-to-anonymous fallback into a hard outage.
<autoloop-decisions-header version="1" block_seq="19" owner="implementer" phase_id="ranking-settings-and-collisions" pair="implement" turn_seq="1" run_id="run-20260326T020604Z-abe9114e" ts="2026-03-26T03:15:53.666674+00:00" />
Keep all persisted viewer controls in one versioned localStorage document so layer toggles, likely-visible-only, heading/pitch nudges, FOV adjustment, and onboarding completion reload together without partial resets.
Compute center-lock from the full projected visible object set first, then run label placement as a second pass with per-type budgets, the PRD priority ladder, viewport clamping, and greedy anchor selection so the bottom dock and displayed labels stay deterministic under dense collisions.
When the stage reports a zero-sized layout box, fall back to the default mobile viewport for projection and label placement instead of collapsing ranking math into a 1x1 viewport during SSR/jsdom/offscreen measurement gaps.
Represent daylit non-priority planets as scene objects with `metadata.daylightLabelSuppressed = true` so center-lock can still target them while the HTML label pass only reveals them when centered or explicitly selected.
<autoloop-decisions-header version="1" block_seq="20" owner="test_author" phase_id="ranking-settings-and-collisions" pair="test" turn_seq="1" run_id="run-20260326T020604Z-abe9114e" ts="2026-03-26T03:33:41.781352+00:00" />
Lock AC-3 with a viewer-shell remount test that changes layer toggles, likely-visible-only, and heading/pitch/FOV controls through the settings sheet, then reloads from localStorage to prove the single persisted settings document survives a full session restart.
Include `tests/unit/**/*.test.{ts,tsx}` in Vitest discovery so the checked-in TSX settings suites actually run under `npm test`; leaving the runner on `*.test.ts` would silently drop the ranking/settings persistence and control-surface regression checks.
<autoloop-decisions-header version="1" block_seq="21" owner="implementer" phase_id="demo-polish-and-verification" pair="implement" turn_seq="1" run_id="run-20260326T020604Z-abe9114e" ts="2026-03-26T03:38:10.291291+00:00" />
Keep built-in demo scenarios addressable through a `demoScenario` route parameter and feed them through the same viewer shell with seeded `AircraftApiResponse`/`TleApiResponse` payloads, so demo mode stays offline-safe and deterministic without forking the projection/ranking/detail-card pipeline.
Make the overlay chrome pointer-events-none by default, opt real panels back into pointer events, and skip manual-pan drag arming when a label button is the pointer target, so desktop/manual fallback still allows label taps to open detail cards.
Vendor the JS-only `satellite.js` entry points behind a local shim instead of importing the package root, because the root export pulls in WASM worker paths that break or stall the Next.js build while the JS SGP4 path satisfies v1.
