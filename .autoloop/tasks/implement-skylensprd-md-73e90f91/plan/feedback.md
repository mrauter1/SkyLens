# Plan ↔ Plan Verifier Feedback

- 2026-03-26: Replaced the placeholder plan with an implementation-ready greenfield plan aligned to the approved PRD, including locked contracts, ordered milestones, regression controls, risk register, and a seven-phase machine-readable decomposition matching the PRD milestone order.
- PLAN-001 | blocking | The plan names `GET /api/config` as a locked public contract in [plan.md](/workspace/SkyLens/.autoloop/tasks/implement-skylensprd-md-73e90f91/plan/plan.md#L107) but never assigns it to any milestone, deliverable, acceptance criterion, validation step, or rollback path in either [plan.md](/workspace/SkyLens/.autoloop/tasks/implement-skylensprd-md-73e90f91/plan/plan.md#L20) or [phase_plan.yaml](/workspace/SkyLens/.autoloop/tasks/implement-skylensprd-md-73e90f91/plan/phase_plan.yaml#L5). Concrete failure mode: implementation can follow the phase plan and still omit `/api/config`, which would drop the required `SKYLENS_BUILD_VERSION` surfacing and the PRD-defined defaults/satellite-groups response. Minimal correction: place `/api/config` in an explicit phase, add its response-shape validation/acceptance criteria, and include rollback notes for this public interface.
- PLAN-002 | blocking | The PRD makes privacy/trust copy mandatory on the landing and live experience (`Camera stays on your device.` and `Location is used only to calculate what is above you right now.`), but the plan only mentions privacy as a general constraint in [plan.md](/workspace/SkyLens/.autoloop/tasks/implement-skylensprd-md-73e90f91/plan/plan.md#L142) and does not make the required UI copy part of any milestone or acceptance criteria in [plan.md](/workspace/SkyLens/.autoloop/tasks/implement-skylensprd-md-73e90f91/plan/plan.md#L20) or [phase_plan.yaml](/workspace/SkyLens/.autoloop/tasks/implement-skylensprd-md-73e90f91/plan/phase_plan.yaml#L5). Concrete failure mode: the app could ship with working permissions and overlays but without the mandated trust messaging, violating explicit product requirements. Minimal correction: add the landing/view privacy reassurance copy and its validation to the app-shell phase deliverables and acceptance criteria.
- PLAN-003 | non-blocking | The narrative introduces a shared `lib/cache` boundary in [plan.md](/workspace/SkyLens/.autoloop/tasks/implement-skylensprd-md-73e90f91/plan/plan.md#L119), but no phase owns introducing that module in [phase_plan.yaml](/workspace/SkyLens/.autoloop/tasks/implement-skylensprd-md-73e90f91/plan/phase_plan.yaml#L103). This is not fatal, but it leaves cache ownership slightly ambiguous between the TLE and aircraft phases. Minimal correction: assign `lib/cache` explicitly to the first backend-data phase that creates it, then reuse it in the later backend phase.
- 2026-03-26: Updated the plan to resolve the verifier gaps by making `/api/config` an explicit M1 contract with validation and rollback coverage, making the required privacy/trust copy part of the app-shell acceptance criteria, and assigning `lib/cache` ownership to the satellite/TLE phase.
- PLAN-004 | non-blocking | Re-verified the revised plan and found that PLAN-001, PLAN-002, and PLAN-003 are now addressed in the current artifacts: `/api/config` is explicitly owned by M1 with validation and rollback, the required trust copy is part of the M1 scope and acceptance criteria, and `lib/cache` ownership is assigned to the satellite/TLE phase. No further findings.
- 2026-03-26: Tightened the implementation contract around exact PRD payload semantics, degraded-mode handling, route splitting, per-type label budgets, and the required named fixtures/scripts so later execution phases cannot satisfy the milestone flow while still missing locked interfaces or verification inventory.
- PLAN-005 | blocking | The plan never locks the required per-object detail-card content from PRD section 14.7 into any milestone, interface definition, acceptance criterion, or validation step. Evidence: [plan.md](/workspace/SkyLens/.autoloop/tasks/implement-skylensprd-md-73e90f91/plan/plan.md) mentions “detail-card behavior” generically in M3/M5 and “metadata-driven detail rendering” in the client contract, but it does not specify the required aircraft/satellite/celestial/star/constellation field sets; [phase_plan.yaml](/workspace/SkyLens/.autoloop/tasks/implement-skylensprd-md-73e90f91/plan/phase_plan.yaml) likewise assigns no deliverable or acceptance criterion for those locked card contents. Concrete failure mode: implementation can satisfy the current plan with generic cards that omit required fields such as aircraft heading/speed/origin country, satellite NORAD/range/ISS badge, or constellation summary text, violating explicit product requirements without failing plan acceptance. Minimal correction: add a locked detail-card payload contract or milestone-owned acceptance criteria/tests that enumerate the required fields for each supported object type.
- PLAN-006 | blocking | The PRD requires an `Enter demo mode` action inside the settings sheet, but the plan never assigns that control to any phase. Evidence: [plan.md](/workspace/SkyLens/.autoloop/tasks/implement-skylensprd-md-73e90f91/plan/plan.md) includes landing/demo entry and demo scenarios, and M6 covers settings persistence plus fix-alignment/recenter, yet it omits the settings-sheet demo action; [phase_plan.yaml](/workspace/SkyLens/.autoloop/tasks/implement-skylensprd-md-73e90f91/plan/phase_plan.yaml) mirrors that omission in the ranking/settings phase and no later phase claims ownership of a settings-based demo entry path. Concrete failure mode: the product could ship with demo mode accessible only from landing/fallback flows, missing a required in-view recovery/control path once the user is already in the viewer. Minimal correction: make `Enter demo mode` an explicit settings-sheet deliverable with acceptance criteria covering its availability from `/view` and its handoff into the shared demo pipeline.
- 2026-03-26: Updated the plan to close PLAN-005 and PLAN-006 by adding a locked per-object detail-card contract to the narrative plan, assigning detail-card field ownership and validation to the celestial/satellite/aircraft phases, and making the settings-sheet `Enter demo mode` action an explicit M6 scope item with acceptance criteria tied to the shared demo pipeline.
- PLAN-007 | non-blocking | Re-verified the updated plan and found that PLAN-005 and PLAN-006 are addressed in the current artifacts: [plan.md](/workspace/SkyLens/.autoloop/tasks/implement-skylensprd-md-73e90f91/plan/plan.md) now locks the detail-card contract per object type and makes the `4°` center-lock radius explicit, and [phase_plan.yaml](/workspace/SkyLens/.autoloop/tasks/implement-skylensprd-md-73e90f91/plan/phase_plan.yaml) now assigns detail-card field ownership plus the settings-sheet `Enter demo mode` action to concrete phases with acceptance criteria. No further findings.
