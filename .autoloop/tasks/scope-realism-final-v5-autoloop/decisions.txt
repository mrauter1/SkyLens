<autoloop-decisions-header version="1" block_seq="1" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260406T193734Z-735ed894" ts="2026-04-06T19:37:35.168430+00:00" />
Treat viewport height as the source input for scopeLensDiameterPct, but clamp the final diameter to viewport-safe bounds so portrait layouts do not overflow horizontally.
Apply the new deep-star aperture dimming only after the existing scope render profile is computed; do not move it into limiting-magnitude or earlier inclusion gates.
Keep haloPx in the deep-star canvas payload for compatibility, but the renderer contract for this task is single-pass core-only drawing.
<autoloop-decisions-header version="1" block_seq="2" owner="planner" phase_id="task-global" pair="plan" turn_seq="2" run_id="run-20260406T193734Z-735ed894" ts="2026-04-06T19:41:10.096987+00:00" />
Keep extended-object scope size inputs local to `viewer-shell.tsx` via one body-id-keyed baseline angular-size table; do not widen `SkyObject` or celestial metadata for this task.
Compute extended-object scope diameter from the final clamped lens diameter and effective scope vertical FOV, then apply family-specific usability clamps instead of scattered fixed marker constants.
Treat `viewer-shell.test.ts`, `viewer-shell-celestial.test.ts`, and `scope-lens-overlay.test.tsx` as the explicit broader regression watchlist when Phase 2 changes lens sizing or bright-object scope behavior.
<autoloop-decisions-header version="1" block_seq="3" owner="implementer" phase_id="settings-and-ui" pair="implement" turn_seq="1" run_id="run-20260406T193734Z-735ed894" ts="2026-04-06T19:42:57.204003+00:00" />
Keep `scopeLensDiameterPct` normalization centralized in `SkyLensServerless/lib/viewer/settings.ts`; `viewer-shell.tsx` reuses the exported normalizer for immediate UI-driven state updates so runtime and persisted values cannot drift.
<autoloop-decisions-header version="1" block_seq="4" owner="test_author" phase_id="settings-and-ui" pair="test" turn_seq="1" run_id="run-20260406T193734Z-735ed894" ts="2026-04-06T19:49:58.657896+00:00" />
Cover the viewer-shell side of `scopeLensDiameterPct` with a mocked `SettingsSheet` callback test in `viewer-shell.test.ts` so clamp-on-callback regressions are caught without depending on dialog DOM behavior.
<autoloop-decisions-header version="1" block_seq="5" owner="implementer" phase_id="scope-runtime-realism" pair="implement" turn_seq="1" run_id="run-20260406T193734Z-735ed894" ts="2026-04-06T19:52:31.894343+00:00" />
Clamp runtime scope lens diameter from `viewport.height * scopeLensDiameterPct` against `min(viewport.width - 32, viewport.height - 32, 440)` so the lens follows the height-based setting where possible but never overflows the active viewport.
Keep deep-star aperture dimming display-only by applying it only when building `scopeStarCanvasPoints`; the stored `scopeRender` metadata remains the post-photometry baseline profile for compatibility and existing detail surfaces.
Use one `viewer-shell.tsx` body-id baseline angular-size table with visibility-biased fallback planet diameters so Sun/Moon use average apparent sizes while planets still show deterministic size growth under the clamped scope FOV.
When the supported `scopeLensDiameterPct` range would fully saturate against viewport safety on tall portrait layouts, remap the supported percentage interval into the safe lens-pixel envelope instead of flat-clamping every supported value to the same diameter.
<autoloop-decisions-header version="1" block_seq="6" owner="test_author" phase_id="scope-runtime-realism" pair="test" turn_seq="1" run_id="run-20260406T193734Z-735ed894" ts="2026-04-06T20:13:10.154988+00:00" />
Stabilize broad `viewer-shell-celestial.test.ts` assertions that are not about animated scene-clock semantics by forcing coarse timer control inside those tests instead of depending on environment-specific `requestAnimationFrame` behavior.
<autoloop-decisions-header version="1" block_seq="7" owner="test_author" phase_id="verification" pair="test" turn_seq="1" run_id="run-20260406T193734Z-735ed894" ts="2026-04-06T20:19:53.146092+00:00" />
Add a narrow-portrait `viewer-shell-scope-runtime.test.tsx` case for `scopeLensDiameterPct` so the supported `50..90` slider range is proven to remap monotonically into the viewport-safe lens envelope instead of collapsing to one flat-clamped diameter.
