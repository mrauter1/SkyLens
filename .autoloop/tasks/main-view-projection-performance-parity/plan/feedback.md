# Plan ↔ Plan Verifier Feedback
- 2026-04-08: Replaced the empty plan with a three-phase implementation plan anchored to the current `viewer-shell` seams: first unify non-scope projection, then add deep-star governance and B-V parity, then tighten regression coverage and reporting. This keeps scope-mode behavior isolated while directly covering the request’s IC checkpoints, hot-path fix, conservative startup rules, and governor evidence requirements.
- PLAN-001 [blocking]: The deep-star preference contract is not implementation-safe yet. In [plan.md](/workspace/SkyLens/.autoloop/tasks/main-view-projection-performance-parity/plan/plan.md) Milestone 2 and Interface definitions, `ViewerSettings.deepStarsEnabled` is described as a persisted preference while the settings surface is still framed as an explicit control/toggle and the required precedence is `user explicit toggle > adaptive governor > device/default heuristic`. A binary persisted toggle does not define how a user returns to the non-explicit `auto` state after overriding it, so an implementation can easily lock the viewer into a permanent user override and bypass the required adaptive/default path. Minimal correction: define the exact persisted/runtime/UI contract for `on`, `off`, and `auto` behavior, including how legacy missing values hydrate, how users re-enter `auto`, and how diagnostics source labels map to that contract.
