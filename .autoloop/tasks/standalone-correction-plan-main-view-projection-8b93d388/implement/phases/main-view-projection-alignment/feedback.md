# Implement ↔ Code Reviewer Feedback

- Task ID: standalone-correction-plan-main-view-projection-8b93d388
- Pair: implement
- Phase ID: main-view-projection-alignment
- Phase Directory Key: main-view-projection-alignment
- Phase Title: Main-View Projection Alignment
- Scope: phase-local authoritative verifier artifact

- IMP-001 `blocking` — Validation gap for AC-2 / deep-star alignment: the phase contract explicitly requires targeted unit/runtime coverage for non-scope deep stars sharing the magnified, camera-frame-aware main-view projector, but the implementation only added tests in `SkyLensServerless/tests/unit/projection-camera.test.ts`, `SkyLensServerless/tests/unit/celestial-layer.test.ts`, and `SkyLensServerless/tests/unit/viewer-shell-celestial.test.ts`. The existing runtime suite that exercises main-view deep stars (`SkyLensServerless/tests/unit/viewer-shell-scope-runtime.test.tsx`) was neither extended nor made runnable under normal validation, so a regression where deep stars still diverge from bright-object markers under cropped live-camera projection would ship untested. Minimal fix direction: add or extend a runnable runtime/unit test that asserts non-scope deep stars co-locate with bright-object or center-lock projection under magnified main view, and make that test part of the standard validation path (either by moving/including the suite under the configured `vitest` glob or by adding equivalent coverage in an already-discovered test file).
- IMP-002 `blocking` — `SkyLensServerless/tests/unit/viewer-shell-scope-runtime.test.tsx:387` / AC-5 validation still does not hold on rerun: the new deep-star co-location test now targets the right runtime path, but the recorded validation command is not stable. Re-running `../node_modules/.bin/vitest run --config vitest.config.ts tests/unit/viewer-shell-scope-runtime.test.tsx` from `SkyLensServerless/` failed in review because the new assertion compares marker coordinates with `toBeCloseTo(..., 2)` and still exceeded the tolerance (`0.007648... > 0.005`). That leaves the serverless runtime validation flaky/non-passing, so the phase cannot be marked complete yet. Minimal fix direction: make the new alignment assertion deterministic against the renderer’s actual float precision, for example by comparing against an explicit epsilon comfortably above observed DOM rounding noise or by normalizing both coordinates before assertion, then re-run the same serverless command and update the notes with the passing result only.
- IMP-003 `non-blocking` — Re-review resolution note: `IMP-001` and `IMP-002` are resolved in the current implementation state. The serverless runtime suite now includes runnable AC-2 coverage in `SkyLensServerless/tests/unit/viewer-shell-scope-runtime.test.tsx`, the alignment assertion uses an explicit pixel epsilon instead of the flaky decimal-place matcher, and re-review passed both `pnpm exec vitest run tests/unit/projection-camera.test.ts tests/unit/celestial-layer.test.ts tests/unit/viewer-shell-celestial.test.ts` and `../node_modules/.bin/vitest run --config vitest.config.ts tests/unit/viewer-shell-scope-runtime.test.tsx` (from `SkyLensServerless/`), with `git diff --check` clean.
